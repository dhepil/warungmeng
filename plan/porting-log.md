# Porting Log

Plain-language audit trail of the AntD → new-target port. **One line per ported
file.** This is the human-readable history of what came from where, so drift is
traceable without reading code.

Format:

```
YYYY-MM-DD | <phase> | <slice> | AntD: <source path> → dest: <destination path> | check: green
```

- `check: green` is only written after `npm run check` passed AND the slice was
  committed. If a slice failed, do not write a green line — note the failure instead.
- The AntD source layout differs from new-target (see plan/plan.json forbiddenPaths),
  so "→ dest" almost always means the code was **reorganized**, not copied verbatim.

---

## Phase 0 — Guardrail harness (no product code)

2026-07-29 | P0 | harness | (no AntD source) → dest: plan/, package.json, CLAUDE.md | check: pending verification

## Phase 1+ — Port slices (append below as they happen)

2026-07-29 | P1-domain | S1 catalog | AntD: packages/domain/src/catalog/{types,validation,variantSelectionRule}.ts → dest: packages/domain/src/catalog.ts (+ package.json, tsconfig.json) | check: green
2026-07-29 | P1-domain | S2 orders | AntD: packages/domain/src/orders/{types,transitions}.ts → dest: packages/domain/src/orders.ts | check: green
2026-07-29 | P1-domain | S3 inventory | AntD: packages/domain/src/inventory/{types,units,stock}.ts → dest: packages/domain/src/inventory.ts (hpp.ts deferred to finance S4) | check: green
2026-07-29 | P1-domain | S4 finance | AntD: packages/domain/src/finance/{types,validation,ledger,calculations}.ts + inventory/hpp.ts → dest: packages/domain/src/finance.ts | check: green
2026-07-29 | P1-domain | S5 reporting | AntD: packages/domain/src/reporting/{types,dashboard,reports}.ts → dest: packages/domain/src/reporting.ts | check: green
2026-07-30 | P1-domain | S6 index+tests | AntD: catalog/orders/inventory/finance *.test.ts → dest: packages/domain/src/index.ts + domainRules.test.ts (17 tests) | check: green (structure+boundaries+typecheck+tests)
2026-07-30 | P1-domain | S6 fix | structuredClone → pure JSON deep clone in finance.ts (domain purity: no DOM lib) | check: green
2026-07-30 | P1-domain | PHASE DONE | all P1 exact files exist, 4/4 checks green, status flipped to done
2026-07-30 | P2-module-system | S1 foundation types | AntD: packages/module-system/src/contracts/{moduleSurface,moduleId,moduleDependency,moduleCapability,moduleManifest,moduleExtension,moduleContribution,moduleDiagnostic}.ts → dest: packages/module-system/src/{operationResult,engineContracts}.ts (+ package.json, tsconfig.json). Dropped surface vocabulary (admin/storefront) and UI vocabulary (navigation/route/label/icon) per new-target LOGIC §5/§11 — generic runtime must know neither. | check: green
2026-07-30 | P2-module-system | S2 registries | AntD: packages/module-system/src/capabilities/{capabilityRegistry,createCapabilityRegistry,registerCapability,resolveCapability}.ts + registry/{moduleRegistry,createModuleRegistry,registerModule}.ts → dest: packages/module-system/src/{capabilityRegistry,engineRegistry}.ts. Reshaped from SOURCE's async extension model to the target's synchronous parent/child creation (LOGIC §3). Kept: staged rollback, provides/publishes match, reverse-order disposal, sibling isolation, idempotent initialize/dispose. | check: green
2026-07-30 | P2-module-system | S3 graph+discovery | AntD: packages/module-system/src/registry/{validateModuleGraph,resolveModuleOrder}.ts + discovery/{discoverModuleCandidates,moduleCandidate,moduleDiscoveryResult}.ts → dest: packages/module-system/src/{dependencyGraph,discovery}.ts. Edges from requires→provides (target has no dependsOn per LOGIC §9). Dropped UI-field + surface validation per LOGIC §5/§11. Added orphan-child and contested-capability checks the parent/child model needs. | check: green
2026-07-30 | P2-module-system | S4 diagnostics | AntD: packages/module-system/src/diagnostics/{createModuleDiagnosticCollector,moduleDiagnosticSink}.ts + the dedupe filter inlined in registry/createModuleRegistry.ts → dest: packages/module-system/src/diagnostics.ts (+ engineRegistry.ts now collects through it). Added grouping by child/engine, severity filter, and a summary with a fatal signal; dedupe made opt-out and left OFF in the registry (its report sites are already distinct — fan-in dedupe belongs to the P3 host). Severity kept uniformly "error" as in SOURCE: "may we continue" is already carried by the degraded status. | check: green
2026-07-30 | P2-module-system | S5 barrel+tests | AntD: packages/module-system/src/index.ts + tests/{capabilityRegistry,moduleDependencyGraph,moduleManifest,moduleRegistry,moduleSurfaceBoundary}.test.ts → dest: packages/module-system/src/{index,moduleSystem.test}.ts. Barrel hand-picks named exports (SOURCE's own style); capabilityRegistry deliberately not exported — it is the staging seam engineRegistry owns. 51 tests written against the public barrel so a missing/renamed export fails. Dropped SOURCE's moduleSurfaceBoundary suite (re-implemented an import scanner in a test; `npm run check` already owns that rule) and its surface/dependsOn/optional-dependency/UI-field cases (concepts the target removed). Also completed the S4 severity correction: removed the last hardcoded "warning" in engineRegistry; all severities now derive from the one map in diagnostics.ts. Mutation-checked the two load-bearing tests — both fail when the behavior is broken. | check: green
2026-07-30 | P2-module-system | PHASE DONE | all 11 P2 exact files exist, 4/4 checks green (68 tests), status flipped to done
P3 S1 | SOURCE apps/admin/src/app/composition/{createAdminRepositories,createAdminCapabilities}.ts + features/orders/application/ports/*.ts (shapes only) → packages/admin-engine/{package.json,tsconfig.json,src/adminEngineContracts.ts,src/adminEngineSnapshot.ts,src/shared/atomicOperationPort.ts} | SOURCE threaded an atomicTransaction through its composition root by hand; the target delivers the same guarantee as a declared capability, with the port→capability seam kept in one file so the two halves cannot drift apart. No UI vocabulary, no area-specific types at the root. | check: green
P3 S2 | SOURCE apps/admin/src/app/composition/* + packages/module-system/src/discovery (startup ORDER and glob shape only, per LOGIC §7) → packages/admin-engine/src/{createAdminEngine.ts,discoverAdminLogic.ts,index.ts} (+ diagnostics field added to adminEngineContracts/adminEngineSnapshot) | The engine root delegates every judgement it can — validity to discovery, eligibility and order to the dependency graph, child creation to the registry — and keeps only what the generic runtime cannot do: merging the three stages' diagnostics with de-duplication, and republishing the injected atomic port as the capability two children require. The republish rides the normal child-publish path rather than widening the registry, so staged rollback stays trustworthy. Discovery is scoped to this package's own engines/ folder, making LOGIC §7 rule 9 structural rather than merely intended. Two mechanics settled by experiment: import.meta.glob must appear literally (aliasing throws at runtime though it type-checks) and needs an ImportMeta augmentation for tsc; zero matches return {} so an empty engines/ folder starts cleanly. Mutation round caught a vacuous de-duplication test and a false-alarm diagnostic; both fixed. | check: green
P3 S3 | SOURCE apps/admin/src/features/menu/application/{catalogReadCapability,menuListModel,variantGroupListModel,menuEditorModel,menuCategoryCommands,variantCategoryEditorModel,variantOptionCommands,variantGroupConnections}.ts + the load/write halves of {useMenuList,useVariantGroupList}.ts + the orchestration inlined in screens/{MenuListScreen,MenuEditorScreen,VariantCategoryEditorScreen}.tsx + the field rules that existed only in components/{MenuEditorDetailsFields,MenuCategoryEditorDialog,VariantCategoryOptionFields}.tsx + the sort rule from packages/data/src/mocks/InMemoryMenuCatalogRepository.ts → packages/admin-engine/src/engines/menu/{menuEngine.ts,menuContracts.ts,children/catalog-read/*,children/menu-editor/*,children/variant-management/*} | First real operational area, landed as three green commits (69b560d, d14ffb5, 8513852). All three children resolve the injected catalog-store port themselves rather than depending on catalog-read, because LOGIC §8 shows no menu child requiring anything. Ordering moved out of the store into the read child (a different adapter would otherwise change menu order silently); form-only rules (name/description limits, "keep at least one option") moved into logic per LOGIC §13; the save baseline is re-read at save time rather than captured at mount. Fixed the inline option-delete invariant hole — it never consulted the selection rule, so it could leave a group demanding more selections than it had options; the availability toggle is guarded too, since the rule is judged against AVAILABLE options. A variant group's two-phase save stays non-atomic (LOGIC §10 scopes the atomic port to the two multi-owner workflows) but now attempts every menu and reports degraded naming the failed ones. Dangling variantGroupIds after a delete left alone deliberately, as in SOURCE. Dropped the dead MenuListQuery search, the repository Proxy singleton, and the hooks' uncached state. DeleteMenuCategoryResult folded into OperationResult. Tests resolve capabilities through a probe child declaring them in requires. 18 mutations caught; on-disk discovery verified with a throwaway test that fails when a child file is mis-suffixed. | check: green (196 tests)
P3 S4 | SOURCE apps/admin/src/features/inventory/application/{useInventoryMaterials,useInventoryMovements}.ts + the rules that existed only in components/{InventoryMaterialEditorDialog,InventoryMovementDialog}.tsx as AntD props + the inline low-stock rule in components/InventoryMaterialsTable.tsx + recordMovement/createIngredient/updateIngredient/archiveIngredient and the sort+filter logic from packages/data/src/mocks/InMemoryInventoryRepository.ts → packages/admin-engine/src/engines/inventory/{inventoryEngine.ts,inventoryContracts.ts,children/materials-read/*,children/stock-movements/*,children/stock-adjustment/*} | Inventory part one, landed as three green commits (9b9fad9, 69628a7, 0af1ccf). The port was INVERTED rather than copied: SOURCE's recordMovement computed the delta, mutated the balance, recomputed average cost and appended the ledger row inside the adapter; the target's commitMovement persists a plan that planStockMovement has already validated and computed. That primitive sits in the area contracts, not in stock-adjustment, because LOGIC §8 shows neither S5 child requiring a sibling capability — the one deliberate exception to "contracts hold no behavior", explained at length in the file and flagged as tech-debt D10. Consolidated the low-stock rule from four disagreeing copies into one: SOURCE badged an ingredient with no balance row as low and then HID it when the low-stock filter was ticked, so the filter excluded exactly the rows it appeared to select. Resolved in favour of the badge, with hasBalanceRecord preserving "never counted" vs "empty". Fixed the orphan-row hazard — a refused movement now writes nothing, where SOURCE created a zero balance row before validating and thereby changed that ingredient's future filter behavior. Ordering moved out of the store into both read children with tie-breaks added (ingredient names are not unique; the automated paths stamp every row of one order with an identical timestamp, so the ledger had no total order). Form-only rules moved into logic: quantity minimum, two-decimal rounding, unit compatibility, purchase-requires-cost, no negative cost, no re-archiving. Rounding adopted the domain's EPSILON convention so one input cannot round two ways. Typed results replace SOURCE's single generic toast. Dropped: the outletId that scoped nothing unless lowStockOnly was set, and the hooks' opaque boolean error flag. 8 mutations across the three children — 7 caught immediately; one exposed a test that only checked half of a count relaxation, which was strengthened and re-verified. On-disk discovery verified with a throwaway test, since the structure checker sees a mis-suffixed file only when the new name matches no allowed glob — a rename to *.test.ts passes structure and typecheck while loading as nothing. | check: green (268 tests)
P3 S5 | SOURCE packages/data/src/mocks/InMemoryInventoryRepository.ts {consumeOrder,revertOrderConsumption,calculateHpp,listRecipes} + apps/admin/src/features/inventory/application/{useInventoryHpp.ts,ports/catalogReadPort.ts} + the retry path in features/pos/application/usePosCashier.ts + the refund gate in features/orders/application/commands/cancelOrderCommand.ts + packages/domain/src/inventory/hpp.ts (already ported into finance.ts in P1) → packages/admin-engine/src/engines/inventory/{inventoryContracts.ts (extended),children/stock-consumption/*,children/stock-reversal/*,children/hpp-calculation/*} | Inventory part two, landed as four green commits (47ece49, 65a064f, ba49026, a19ba0a); the area is now complete at six children. Closed tech-debt D14 (referenceId on MovementStoreQuery — SOURCE scanned the whole movement array on every checkout) and half of D16 (listRecipes added; saveRecipe restated as the remaining gap, since LOGIC §8 names no child owning recipe editing). Added commitMovements so a multi-row consumption is one call the caller's atomic boundary can wrap — SOURCE looped single writes, so a failure partway left stock decremented with no way to finish, and its guard then latched on the partial set and returned it forever. Both write children go through S4's planStockMovement rather than reimplementing invariants, which is the payoff for placing it at area level. One interaction S4 could not see: that primitive's 0.01 floor and 2dp rounding are rules about what a PERSON may type, and a consumption quantity is derived from recipe arithmetic that can legitimately be 0.001 — so the input gained QuantitySource and only those two rules branch on it; threading it through the S4 child changed no behavior (268 tests passed unchanged). Fixed: SOURCE's dry run validated LESS than its write (existence only, versus the write also refusing archived ingredients), so an archived ingredient passed the projection and threw mid-loop leaving a half-consumed order — no separate projection exists now; the idempotency guard returned rows silently so POS's retry reported success for an order it never finished (outcome now carries `replayed`); an order with no recipes wrote nothing and never latched the guard, so every retry re-ran it. The sharpest correction is the reversal: SOURCE re-derived from the consumed row's entered quantity and unit against the ingredient's CURRENT definition and never read the stored baseQuantityDelta, so a g→kg edit between consume and cancel restored 1000x and a g→ml edit threw and left the order permanently un-cancellable behind a retryable-but-never-succeeding failure; negating the stored delta is immune to both. hpp-calculation is the first child in the port with a `requires` (admin.menu.catalog-read), so its test is the first to compose two areas and to prove the dependency graph excludes the child when the required area is absent; it also resolves SOURCE's two contradictory resilience policies over one dataset (HPP screen all-or-nothing, dashboard per-item) in favour of degrading, and names the pricing defaults that were library default arguments. Dropped SOURCE's N+1 recipe refetch. New debt: D18 (unpaid consumed order never reversed — slice 8's trigger decision), D19 (triple rounding, pairs with D11), D20 (dashboard COGS restates history when a cost moves — slice 11; fix is to store unitCost on the consumption row, which writes null as SOURCE did), D21, D22. 9 mutations across three children, all caught. Two of my own test assumptions were wrong and the tests caught them: floating-point 220.00000000000003 from SOURCE's own expression, and positional row indexing that ignored the Menu area's ordering promise. | check: green (323 tests)
P3 S6 | SOURCE apps/admin/src/features/finance/application/{financeCapabilities,financeDateRange,financeRefundProjection,useFinanceLedger,useFinanceOverview,useFinanceTransactionEditor,useFinanceTransactions}.ts + screens/{FinanceOverviewScreen,FinanceTransactionListScreen,FinanceExpenseScreen}.tsx + components/{FinanceTransactionEditorDialog,FinanceTransactionTable,FinanceTransactionToolbar,FinanceExpenseBreakdown,FinancePaymentMethodBreakdown}.tsx + packages/data/src/{repositories/FinanceRepository.ts,mocks/InMemoryFinanceRepository.ts} + cross-consumers in dashboard/orders/pos; domain finance math already ported in P1 → packages/admin-engine/src/engines/finance/{financeEngine.ts,financeContracts.ts,children/ledger-read/*,children/transaction-recording/*,children/expense-management/*,children/refund-projection/*} | Finance landed as six green commits (6d603b5, 40d2308, 9b91302, 0dc6370, 77ffed8, 9f67f13). Preserved SOURCE's strongest design: only manual transactions are stored; automatic sales/refunds derive from orders with deterministic ids. Ledger-read independently loads both sources, degrades when one survives, fails when none do, owns all filtering/sorting, and consolidates local/UTC/Jakarta date disagreement on Jakarta. Transaction-recording moves form-only rules into logic, closes unknown/custom category validation gaps, uses tagged authoritative write outcomes (no pre-read race), and reports replayed voids. Expense-management remains a pure posted-outflow projection because LOGIC gives it no sibling dependency; fixes table/total mismatch. Refund-projection is pure and states refundable means money, not stock. Dropped dead get-by-id, id generator, store-side query; domain input is aliased not redefined. New debt D23-D26. 7 mutations caught/restored; allowed-glob rename proved discovery witness; public Vite runtime observed 4/4 active + idempotent disposal. | check: green (371 tests)
P3 S7 | SOURCE packages/data/src/{repositories/OrderRepository.ts,mocks/InMemoryOrderRepository.ts} + apps/admin/src/features/orders/application/{ordersCapabilities,useOrderList,useOrderDetail}.ts + components/OrderListToolbar.tsx + POS/Storefront order-create call sites and domain POS checkout builder → packages/admin-engine/src/engines/orders/{ordersEngine.ts,ordersContracts.ts,children/order-read/*,children/order-submission/*} | Orders part one landed as four green commits (53a9ee1, b19cdcc, 5b06da9, 764d885). Read behavior moved out of storage: search/filter/order owned by the child, stable id tie-break, exact-id typed not-found, date grammar/range validation, and one Jakarta calendar with Finance/reporting instead of SOURCE's UTC-filter/local-display split. Submission stays a complete-order persistence handoff and deliberately excludes POS pricing/session/cart, Storefront cart/customer behavior, inventory, Finance, receipts, cancellation, and atomic orchestration. Backend target's required idempotency key closes SOURCE's duplicate-on-retry seam; authoritative commit distinguishes created/replayed/conflict, with replay observable and invalid input writing nothing. Normal forward status progression has no planned target child and is D28 rather than hidden in read/submission. 6 mutations caught/restored; real-disk discovery found 2/2 Orders children and failed under an allowed-glob rename while structure remained green. | check: green (392 tests)
2026-07-31 | P3 | post-S7 sync (no product code) | toolchain only — no source file ported | **Tech-debt D17 diagnosed, fixed, and retired from the register.** The unexplained `package-lock.json` change first seen in S4 and the vitest startup failure seen this session were ONE event, not two: the repository lives on a Windows drive while the build agent runs under Linux, so an `npm install` from the Windows side installs `@rolldown/binding-win32-x64-msvc`, leaves no `@rolldown/binding-linux-x64-gnu`, and rewrites the lockfile by dropping optional `@emnapi/*` entries. Presented as `npm run check` red on `tests` ALONE with structure/boundaries/typecheck green — that signature means toolchain, not drift, and no product code should be investigated until it is ruled out. Cleared with `git checkout -- package-lock.json` then `npm install --no-save @rolldown/binding-linux-x64-gnu`; `--no-save` is what keeps the restored lockfile clean. Rolldown's own advice (delete lockfile + node_modules, reinstall) was deliberately NOT followed — it regenerates a tracked file and would fold a large unrelated dependency change into a slice commit. Expect recurrence after any Windows-side install. | check: green (392 tests)
P3 S8 | SOURCE apps/admin/src/features/orders/application/commands/cancelOrderCommand.ts + ports/financeRefundPort.ts + ordersCapabilities.ts + screens/OrderDetailScreen.tsx (the Popconfirm and its toast branches) + packages/data/src/mocks/{InMemoryOrderRepository.ts (updateOrderStatus), InMemoryAtomicDataTransaction.ts, InMemoryInventoryRepository.ts (revertOrderConsumption)} + packages/domain/src/orders/transitions.ts (already ported in P1) → packages/admin-engine/src/engines/orders/{ordersContracts.ts (extended),children/order-cancellation/{cancelOrderAtomically.ts,orderCancellationChild.ts,orderCancellation.test.ts}} | Order cancellation landed as two green commits (b4c5b0c, 6455f64); the Orders area is complete at three children and this is the first real consumer of the S1 atomic seam. Tech-debt D18 RESOLVED by owner decision on 2026-07-31: reverse stock whenever it was deducted, not when money was refunded. SOURCE gated the reversal on projectRefund(order).length > 0, which reduces to "was this paid" because the domain only settles paid→refunded on cancellation — so an unpaid order that had consumed stock was cancelled and silently kept the stock deducted, drifting inventory permanently low. The reversal is now attempted unconditionally and the reversal capability is the sole judge of whether anything is owed back; the refund projection is an output and never a gate. The design is shaped by one mechanical fact: inside an atomic boundary a returned failure COMMITS, since rollback is driven by a throw — so a refusal that wrote nothing is returned as a value while any failure after the write throws CancellationRollback and undoes all three owners, and the child converts that back to a normalized failure so the capability still returns rather than throws (LOGIC §5). Calling the reversal unconditionally required classifying a sibling's failures: stock-reversal reports "never consumed" as a failure, correct for its own caller but an ordinary ending here, so never-consumed and already-reversed are benign and everything else rolls back — treating them as fatal would have made most orders impossible to cancel. Also removed SOURCE's updateStatus(orderId, status) bypass, which accepted "cancelled", wired straight to the repository, and could flip a paid order to cancelled/refunded with no reversal outside any transaction; its own doc comment claimed cancel was the single cancellation command while that sibling contradicted it, and the invariant held only because one screen filtered the value out of a .find(). Four endings are now distinct (stock returned, stock already returned, refund owed, already-cancelled vs not-cancellable) where SOURCE had one boolean meaning "was paid" and displayed it as though it meant "stock came back", plus one generic warning for both refusals. Capability id is admin.orders.cancel per LOGIC §8 rather than the child id — the one place in the area they differ — and the probe-child test pattern proved it: publishing under the wrong id failed 16 of 20 tests. New debt D29 (admin.orders.read declared per LOGIC and deliberately never called; the store is the authoritative judge and a pre-read would be a second judge and a race). 20 permanent tests; 7 mutations all caught, including the D18 gate, the swallowed-rollback case, and the wrong capability id. Typecheck caught two fixture defects vitest's transpile-only run could not see. Discovery witness re-confirmed: structure stayed green under an allowed-glob rename while the child loaded as nothing. | check: green (412 tests)
P3 S9 | SOURCE packages/domain/src/pos/{types,session,cart,pricing,checkout}.ts + apps/admin/src/features/pos/application/{posSessionStore,posSessionPersistence,posCashierModel,posCapabilities,usePosCashier,usePosCatalog}.ts + its three ports, composition wiring, and the catalog/cart/variant/session/close components + cashier screen → packages/admin-engine/src/engines/pos/{posEngine.ts,posContracts.ts,children/session/*,children/cart/*} | POS session and cart landed as two green commits (93ec301, 0f4ef83). Bookkeeping note: the code landed at 00:32 and this line, the roadmap tick and the progress note were written by a later session — the building session put its research in p3-handoff.md and stopped before updating the plan-of-record, so the handoff's S9 section carries a status correction rather than being rewritten. Session preserves SOURCE's single open/closed till for one outlet, whole non-negative IDR opening balance, expected cash as opening + cash sales, and a signed close variance, and closes the hole where a second open silently reset an active till and lost the prior session (prevented in SOURCE only by a hidden button; now a named conflict, mutation-tested). Session deliberately does not clear the cart on close although SOURCE did, because LOGIC §8 gives session no cart requirement and cross-child imports are forbidden — cart exposes clear and the close flow's caller composes both capabilities, rather than hiding a cart mutation behind a shared mutable updater. Cart owns line snapshots, quantity, variant configuration, clear and the pure unit/line/subtotal projections only; merge identity is menu id + sorted (groupId, optionId) pairs + trimmed note, so variant order and outer whitespace cannot create duplicate lines while case and internal whitespace still do. Variant resolution walks only the menu's own variantGroupIds, tolerates stale links as missing-group (D1), de-duplicates option ids, checks the domain selection bounds and rejects unknown/unavailable options. Persistence became an injected transport-neutral port instead of SOURCE's single unrestricted update(updater) mirrored into a shallowly-validated sessionStorage blob, per the backend target's position that POS session is production operational state and browser storage only a recovery cache; a missing port keeps the child active and publishing with one diagnostic and normalized dependency failures, and no browser global is imported. Availability reuses the domain isMenuAvailable combined with visibility rather than SOURCE's literal status check, consolidating the D6 family now that S9 is the second strict-ordering reader; schedule/time-zone enforcement was deliberately not invented, since SOURCE has no evaluator, LOGIC defines none, and Settings business hours is S12. Checkout defaults, tax, rounding, cash sufficiency, receipt, cash-sales increment, order number and aggregate construction were all kept out as S10's work. Verified in a later session because the commits recorded no evidence: on-disk discovery finds admin.pos and both children with LOGIC §8 ids verbatim and empty requires, and the witness was proven able to fail — renaming posCartChild.ts to an allowed *.test.ts left structure green while discovery lost the child (restored byte-for-byte, throwaway deleted); the double-open guard fails its test when disabled. Reconciliation variance sign, cart merge identity, variant validation, quantity removal and revision-guarded clear have tests but were not mutation-checked. Carried to S10: nearest-step rounding can yield a negative rounding adjustment while order-submission rejects every negative Money including totals.rounding. | check: green (430 tests)
P3 S10 | SOURCE packages/domain/src/pos/{pricing,checkout}.ts + apps/admin/src/features/pos/application/{usePosCashier,posCheckoutCommand,posCapabilities}.ts + its checkout/payment/receipt components and the cashier screen's submit path + packages/data/src/mocks/{InMemoryAtomicDataTransaction,InMemoryOrderRepository,InMemoryInventoryRepository}.ts → packages/admin-engine/src/engines/pos/{posContracts.ts (extended),children/checkout/{posCheckoutChild.ts,submitPosCheckoutAtomically.ts,posCheckout.test.ts}} (+ order-submission's rounding validator narrowed) | POS checkout landed as three green commits (ffc2132 contracts, 68e1606 child + atomic workflow, 1af89fd the D23 guard test); the POS area is complete at three children and this is the second and last atomic workflow in the port. Seven required capabilities per LOGIC §8 — the widest child in the port — each proven to make the child UNAVAILABLE when absent rather than to fail at call time. **Tech-debt D23 RESOLVED, and S6's recorded strong default held: the sale stays derived.** Checkout declares and resolves admin.finance.transaction-recording so the graph edge is honest, but never calls the manual writer — one writer for one fact, no de-duplication made load-bearing, and no method invented on the capability. It instead VERIFIES the derivation before finalizing the till: project the committed order, demand exactly one row of type `sale`, roll back otherwise. That guard is reachable rather than defensive: checkout stamps `paid`, but the store returns the STORED order, and on an idempotent replay that order may have been cancelled since it was written — the domain settles paid→refunded on cancellation, so a refunded order projects TWO rows and deriving the till's sale from it would book a refund the drawer never paid out. S8's mechanical rule governs every ending and predicted this slice exactly: inside an atomic boundary a RETURNED failure commits because rollback is driven by a throw, so all pre-write refusals (empty cart, unavailable menu, insufficient cash, closed session, cart-changed-during-identity-reservation) return having written nothing, while every post-write failure throws PosCheckoutRollback and undoes all owners, which the child normalizes back to a returned failure per LOGIC §5. S9's carried-forward warning was real and is now closed: nearest-step rounding yields a NEGATIVE rounding adjustment (−49 on the worked example) while order-submission rejected every negative Money, so S10a narrowed that validator to exempt totals.rounding ALONE rather than distorting S9's subtotal behavior or relaxing money validation generally. Cart finalization is revision- and fingerprint-guarded, so items added between submission and clear are preserved instead of destroyed, and the cash-sale increment rides that same call. Order and inventory replays surface as degraded rather than collapsing into success or failure. The mutation round is why this needed a third commit: deleting the D23 guard left all 16 tests passing, because the only finance assertion was "the manual writer was not called" — which proves there is no second writer but says nothing about the row the till depends on. The first replacement test was itself worthless (it called the domain projection directly, where checkout's own `paid` stamp makes it pass with or without the guard) and did not compile; rewritten to drive the guard through the real runtime and then verified able to fail — disabling the guard fails that test and only that test, and the guard was restored byte-for-byte with the diff checked before committing. | check: green (450 tests)
P3 S11 | SOURCE apps/admin/src/features/dashboard/application/{dashboardCapabilities,dashboardModel,useDashboardOverview,useDashboardReports}.ts + dashboard screen consumers + packages/data/src/mocks/InMemoryInventoryRepository.ts consumption write + domain reporting selectors already ported in P1 → packages/admin-engine/src/engines/dashboard/{dashboardEngine.ts,dashboardContracts.ts,children/overview/*,children/reports/*} + packages/admin-engine/src/engines/inventory/children/stock-consumption/* | Dashboard landed as two green commits (8a576d9 overview + D20 capture, 97234d9 reports); the area is complete at two children. Both resolve LOGIC §8 capabilities exactly, keep the parent identity-only, and use the domain reporting module for all aggregation. D15 RESOLVED: no repository-shaped Pick or inventory store import; overview consumes active materials through admin.inventory.materials-read, while reports consumes the movement ledger through admin.inventory.stock-movements and reconstructs current balances from deltas. Orders, Inventory, and Finance degrade independently with upstream issues preserved; all three unusable returns failure rather than a fabricated empty dashboard. Jakarta owns the period. Missing stock balances project as zero and archived materials stay excluded. D20 NARROWED, NOT CLOSED: S11 explicitly touched stock-consumption so new consumption movements snapshot sale-time unit cost in their entered unit (with conversion); legacy null-cost rows degrade reports. Historical item-level COGS remains open because domain reporting still accepts menuHpp and movements only identify an order, while the exact Dashboard graph has no catalog/HPP edge; S11 does not smuggle in current HPP or reopen closed P1 domain. D11/D19 unchanged. Four focused mutations each failed exactly one test; an allowed-glob rename kept structure green while real discovery lost reports, then restoration passed. | check: green (471 tests)
P3 S12 | SOURCE packages/ui-admin/src/theme/{themeTypes,themeDefaults,themeStorage,themeSerialization,themeRandomizer,AdminThemeProvider}.ts + apps/admin/src/features/settings/theme/* + apps/admin/src/features/settings/business-hours/* + packages/data business-hours fixtures/repository behavior → packages/admin-engine/src/engines/settings/{settingsEngine.ts,settingsContracts.ts,children/theme-preference/*,children/business-hours/*} | Settings landed as two green commits (d2e5adf theme preference, de42ceb business hours); the seventh and final operational P3 area is complete with exactly two independent children, exact capability ids, empty requires, and an identity-only parent. Theme preference is schema-v2 headless state behind a transport-neutral injected port, validates before one write, migrates v1 with a degraded issue, defaults safely on empty/malformed persisted data, and imports no UI or browser runtime. Business Hours is backend-owned, explicitly outlet-keyed persistence; it validates complete weekdays, ranges, special schedules, real dates and overlaps, sorts ranges on save, and evaluates regular/special outlet hours together with optional Menu SalesSchedule at an Asia/Jakarta instant. This supplies S9's deferred policy but makes no POS edit: later composition must call it after catalog revalidation and before the first checkout write through an explicit owner-approved graph edge. No D30 because the policy exists; D25 remains because Settings does not choose the active outlet. Missing ports keep children active/published with one diagnostic and normalized call failures. Four focused mutations each failed exactly one test; a real-discovery witness failed under an allowed-glob business-hours rename while structure stayed green, then passed after byte-for-byte restoration. | check: green (495 tests)
P3 S13 | no SOURCE file — this slice ports no behavior. Structural authority read instead: new-target/LOGIC-TARGET-FILE-TREE.md §4 (the seven areas and every planned child file) and §8 (the Admin capability graph), transcribed into the test as literal strings → packages/admin-engine/src/adminEngineGraph.test.ts | The P3 phase gate, landed as one green commit (be938e9), and the last planned P3 file. It exists because `plan.json` lists the `engines/*` entries as `allow` globs while listing this file as `exact`: a partial or entirely missing area passes structure, boundaries, typecheck and the suite, so completeness is not something the structure checker can prove. Every other test in the package injects its definitions — which proves a child behaves correctly when handed to a runtime, but never that the child is REACHABLE in a real build. This is the only test that composes from real on-disk `import.meta.glob` discovery with nothing injected. It proves: all 7 engines and all 23 children are found; every capability id matches LOGIC §8 verbatim, including the two that deliberately differ from their child id (`admin.orders.cancel` and `admin.orders.read`); the five children with requirements declare exactly the doc's edges in the doc's order while the other eighteen require nothing (an invented edge is as much a departure as a missing one, and Settings correctly appears nowhere in §8); the injected atomic port is republished as `admin.atomic-operation`, and without it exactly the two atomic children are excluded while their siblings keep running; a healthy runtime reports ZERO diagnostics (the S2 false-alarm class); providers initialize before consumers; and disposal is idempotent, leaving zero active children with no failures. Two choices drive the file. Ids are asserted as SETS rather than counts, because a renamed file keeps the count identical while loading as nothing and "expected 23, got 22" names no culprit. And every expectation is a LITERAL string rather than an import from the area contracts, because importing `ORDER_CANCEL_ID` would make the test agree with the code by construction — a rename would rename the expectation with it and the test could never fail. Three mutations, all caught and restored byte-for-byte. The decisive one makes permanent what nine prior slices proved with a throwaway: `businessHoursChild.ts` renamed to an allowed `*.test.ts` glob AND its sibling's import fixed so the mutation was entirely silent — structure stayed GREEN at 85 files "where the plan says" and all 497 other tests passed, including the business-hours suite happily testing a child the real runtime never loads, while only this gate failed and named `admin.settings.business-hours` in the diff. The other two: dropping hpp-calculation's LOGIC-required catalog-read edge (3 failures), and publishing cancellation under its child id (4 failures — the mutation that justifies the literal strings). One assumption corrected rather than asserted away: a graph-EXCLUDED child reports `unmetRequirements: []`, because the registry fills that field only for children it actually attempted; the reason lives in the graph's `missing-dependency` diagnostic, so the gate asserts it there and records why an empty array is not "no reason known". Register housekeeping: resolved D18 and D23 deleted per the register's own "delete on the way out" rule; D10, D16, D28 and D29 judged against the complete graph, with D28, D16 and D10 raised to the owner because each would need a `plan.json` addition (rule 7) that no agent may make alone. | check: green (516 tests)
2026-08-01 | P3 | PHASE CLOSED | no source file — status flip only | P3-admin-engine set to `done` and `activePhase` moved to P4-storefront-engine in plan.json. Exactly three lines changed, all status fields, no structure change — the one plan edit CLAUDE.md permits. All 9 `exact` admin-engine files exist, plus 7 area engines, 7 area contracts, 23 children, 23 child tests, and the 2 atomic workflow files. With the phase marked `done` the structure checker begins enforcing that every `exact` P3 file is present, and it passes. P4-storefront-engine is now active but NOT started. | check: green (516 tests)
PD DS-A | DESTINATION-only structural refactor: packages/admin-engine/src/engines/inventory/inventoryContracts.ts and pos/posContracts.ts shared behavior → packages/admin-engine/src/engines/{inventory/inventoryOperations.ts,pos/posOperations.ts}, with every existing caller redirected | D10 RESOLVED in two commits: owner-approved plan checkpoint 53fbba3, then `refactor(admin-engine): DS-A move shared rules to operations`. The plan checkpoint alone set activePhase to P3 and added both required allow globs (`*Operations.ts` and `*Operations.test.ts`) without changing exact entries, forbidden paths, boundaries, or any other plan field. The move relocates exactly `roundEntered`, `recomputeAverageUnitCost`, `planStockMovement`, and `posCartFingerprint`; normalized byte comparison proves each original comment+body block is identical. Constants, tokens, issue maps, defaults, and all types/interfaces remain in contracts. Every area contracts file now exports zero functions and no contracts file imports Operations; dependency is one-way Operations → Contracts. Existing tests merely changed import paths: no new test, no expectation change, no child change, and the untouched S13 phase gate still proves 23 children. D10's live register entry was deleted and the owner table records the resolving commit subject. | check: green — structure 87 files, boundaries, typecheck, 26 test files, exactly 516 tests
PD DS-B | SOURCE packages/data/src/{repositories/OrderRepository.ts,mocks/InMemoryOrderRepository.ts} + apps/admin/src/features/orders/application/{ordersCapabilities,useOrderDetail}.ts + screens/OrderDetailScreen.tsx; closed target domain transition machine imported through the authoritative store contract → new-target/LOGIC-TARGET-FILE-TREE.md §4/§8 + packages/admin-engine/src/engines/orders/{ordersContracts.ts,children/order-progression/{orderProgressionChild.ts,orderProgression.test.ts}} + adminEngineGraph.test.ts | D28 RESOLVED. Owner authorization landed first and alone in 3b12f3b; product landed in 1493d9f. Exact child/capability id is `admin.orders.order-progression`, requiring `admin.orders.read` as a liveness boundary while deliberately making no pre-read call. A narrow `ForwardOrderStatus` and `progressOrder` port structurally exclude `new`/`cancelled`; the child separately rejects runtime `cancelled` before the port, because domain `transitionOrderStatus` would otherwise accept it and refund a paid order, recreating S8's bypass. The store remains authoritative and owns domain transition + clock/event + commit in one write, returning `updated | not-found | invalid-transition`; there is no general setter and no cross-child import. Missing read excludes the child; missing store leaves it active/published with one own diagnostic and normalized failures. Twelve permanent tests reach capability through a probe, and S13 now asserts 24 children plus the exact literal id/read edge. Three mutations each failed exactly one test (cancellation opened and returned success, invalid transition mislabeled success, exception leaked) and restored to SHA-256 8AA70EC3D2EF51F1D6841CAEEAD737B39E0BA781792D487CEA6AC0633232720B. D28 live entry deleted; D29 updated with the actual no-pre-read decision. | check: green — structure 89 files, boundaries, typecheck, 27 test files, 528 tests
