# Porting Log

Plain-language audit trail of the AntD → new-target port. **One line per ported
file.** This is the human-readable history of what came from where, so drift is
traceable without reading code.

Format:

```
YYYY-MM-DD | <phase> | <slice> | AntD: <source path> → dest: <destination path> | check: green
```

- `check: green` is only written after `npm run check` passed AND the slice was
  committed. If a slice failed, do not write a green line — note the failure instead.
- The AntD source layout differs from new-target (see plan/plan.json forbiddenPaths),
  so "→ dest" almost always means the code was **reorganized**, not copied verbatim.

---

## Phase 0 — Guardrail harness (no product code)

2026-07-29 | P0 | harness | (no AntD source) → dest: plan/, package.json, CLAUDE.md | check: pending verification

## Phase 1+ — Port slices (append below as they happen)

2026-07-29 | P1-domain | S1 catalog | AntD: packages/domain/src/catalog/{types,validation,variantSelectionRule}.ts → dest: packages/domain/src/catalog.ts (+ package.json, tsconfig.json) | check: green
2026-07-29 | P1-domain | S2 orders | AntD: packages/domain/src/orders/{types,transitions}.ts → dest: packages/domain/src/orders.ts | check: green
2026-07-29 | P1-domain | S3 inventory | AntD: packages/domain/src/inventory/{types,units,stock}.ts → dest: packages/domain/src/inventory.ts (hpp.ts deferred to finance S4) | check: green
2026-07-29 | P1-domain | S4 finance | AntD: packages/domain/src/finance/{types,validation,ledger,calculations}.ts + inventory/hpp.ts → dest: packages/domain/src/finance.ts | check: green
2026-07-29 | P1-domain | S5 reporting | AntD: packages/domain/src/reporting/{types,dashboard,reports}.ts → dest: packages/domain/src/reporting.ts | check: green
2026-07-30 | P1-domain | S6 index+tests | AntD: catalog/orders/inventory/finance *.test.ts → dest: packages/domain/src/index.ts + domainRules.test.ts (17 tests) | check: green (structure+boundaries+typecheck+tests)
2026-07-30 | P1-domain | S6 fix | structuredClone → pure JSON deep clone in finance.ts (domain purity: no DOM lib) | check: green
2026-07-30 | P1-domain | PHASE DONE | all P1 exact files exist, 4/4 checks green, status flipped to done
2026-07-30 | P2-module-system | S1 foundation types | AntD: packages/module-system/src/contracts/{moduleSurface,moduleId,moduleDependency,moduleCapability,moduleManifest,moduleExtension,moduleContribution,moduleDiagnostic}.ts → dest: packages/module-system/src/{operationResult,engineContracts}.ts (+ package.json, tsconfig.json). Dropped surface vocabulary (admin/storefront) and UI vocabulary (navigation/route/label/icon) per new-target LOGIC §5/§11 — generic runtime must know neither. | check: green
2026-07-30 | P2-module-system | S2 registries | AntD: packages/module-system/src/capabilities/{capabilityRegistry,createCapabilityRegistry,registerCapability,resolveCapability}.ts + registry/{moduleRegistry,createModuleRegistry,registerModule}.ts → dest: packages/module-system/src/{capabilityRegistry,engineRegistry}.ts. Reshaped from SOURCE's async extension model to the target's synchronous parent/child creation (LOGIC §3). Kept: staged rollback, provides/publishes match, reverse-order disposal, sibling isolation, idempotent initialize/dispose. | check: green
2026-07-30 | P2-module-system | S3 graph+discovery | AntD: packages/module-system/src/registry/{validateModuleGraph,resolveModuleOrder}.ts + discovery/{discoverModuleCandidates,moduleCandidate,moduleDiscoveryResult}.ts → dest: packages/module-system/src/{dependencyGraph,discovery}.ts. Edges from requires→provides (target has no dependsOn per LOGIC §9). Dropped UI-field + surface validation per LOGIC §5/§11. Added orphan-child and contested-capability checks the parent/child model needs. | check: green
2026-07-30 | P2-module-system | S4 diagnostics | AntD: packages/module-system/src/diagnostics/{createModuleDiagnosticCollector,moduleDiagnosticSink}.ts + the dedupe filter inlined in registry/createModuleRegistry.ts → dest: packages/module-system/src/diagnostics.ts (+ engineRegistry.ts now collects through it). Added grouping by child/engine, severity filter, and a summary with a fatal signal; dedupe made opt-out and left OFF in the registry (its report sites are already distinct — fan-in dedupe belongs to the P3 host). Severity kept uniformly "error" as in SOURCE: "may we continue" is already carried by the degraded status. | check: green
2026-07-30 | P2-module-system | S5 barrel+tests | AntD: packages/module-system/src/index.ts + tests/{capabilityRegistry,moduleDependencyGraph,moduleManifest,moduleRegistry,moduleSurfaceBoundary}.test.ts → dest: packages/module-system/src/{index,moduleSystem.test}.ts. Barrel hand-picks named exports (SOURCE's own style); capabilityRegistry deliberately not exported — it is the staging seam engineRegistry owns. 51 tests written against the public barrel so a missing/renamed export fails. Dropped SOURCE's moduleSurfaceBoundary suite (re-implemented an import scanner in a test; `npm run check` already owns that rule) and its surface/dependsOn/optional-dependency/UI-field cases (concepts the target removed). Also completed the S4 severity correction: removed the last hardcoded "warning" in engineRegistry; all severities now derive from the one map in diagnostics.ts. Mutation-checked the two load-bearing tests — both fail when the behavior is broken. | check: green
2026-07-30 | P2-module-system | PHASE DONE | all 11 P2 exact files exist, 4/4 checks green (68 tests), status flipped to done
P3 S1 | SOURCE apps/admin/src/app/composition/{createAdminRepositories,createAdminCapabilities}.ts + features/orders/application/ports/*.ts (shapes only) → packages/admin-engine/{package.json,tsconfig.json,src/adminEngineContracts.ts,src/adminEngineSnapshot.ts,src/shared/atomicOperationPort.ts} | SOURCE threaded an atomicTransaction through its composition root by hand; the target delivers the same guarantee as a declared capability, with the port→capability seam kept in one file so the two halves cannot drift apart. No UI vocabulary, no area-specific types at the root. | check: green
P3 S2 | SOURCE apps/admin/src/app/composition/* + packages/module-system/src/discovery (startup ORDER and glob shape only, per LOGIC §7) → packages/admin-engine/src/{createAdminEngine.ts,discoverAdminLogic.ts,index.ts} (+ diagnostics field added to adminEngineContracts/adminEngineSnapshot) | The engine root delegates every judgement it can — validity to discovery, eligibility and order to the dependency graph, child creation to the registry — and keeps only what the generic runtime cannot do: merging the three stages' diagnostics with de-duplication, and republishing the injected atomic port as the capability two children require. The republish rides the normal child-publish path rather than widening the registry, so staged rollback stays trustworthy. Discovery is scoped to this package's own engines/ folder, making LOGIC §7 rule 9 structural rather than merely intended. Two mechanics settled by experiment: import.meta.glob must appear literally (aliasing throws at runtime though it type-checks) and needs an ImportMeta augmentation for tsc; zero matches return {} so an empty engines/ folder starts cleanly. Mutation round caught a vacuous de-duplication test and a false-alarm diagnostic; both fixed. | check: green
P3 S3 | SOURCE apps/admin/src/features/menu/application/{catalogReadCapability,menuListModel,variantGroupListModel,menuEditorModel,menuCategoryCommands,variantCategoryEditorModel,variantOptionCommands,variantGroupConnections}.ts + the load/write halves of {useMenuList,useVariantGroupList}.ts + the orchestration inlined in screens/{MenuListScreen,MenuEditorScreen,VariantCategoryEditorScreen}.tsx + the field rules that existed only in components/{MenuEditorDetailsFields,MenuCategoryEditorDialog,VariantCategoryOptionFields}.tsx + the sort rule from packages/data/src/mocks/InMemoryMenuCatalogRepository.ts → packages/admin-engine/src/engines/menu/{menuEngine.ts,menuContracts.ts,children/catalog-read/*,children/menu-editor/*,children/variant-management/*} | First real operational area, landed as three green commits (69b560d, d14ffb5, 8513852). All three children resolve the injected catalog-store port themselves rather than depending on catalog-read, because LOGIC §8 shows no menu child requiring anything. Ordering moved out of the store into the read child (a different adapter would otherwise change menu order silently); form-only rules (name/description limits, "keep at least one option") moved into logic per LOGIC §13; the save baseline is re-read at save time rather than captured at mount. Fixed the inline option-delete invariant hole — it never consulted the selection rule, so it could leave a group demanding more selections than it had options; the availability toggle is guarded too, since the rule is judged against AVAILABLE options. A variant group's two-phase save stays non-atomic (LOGIC §10 scopes the atomic port to the two multi-owner workflows) but now attempts every menu and reports degraded naming the failed ones. Dangling variantGroupIds after a delete left alone deliberately, as in SOURCE. Dropped the dead MenuListQuery search, the repository Proxy singleton, and the hooks' uncached state. DeleteMenuCategoryResult folded into OperationResult. Tests resolve capabilities through a probe child declaring them in requires. 18 mutations caught; on-disk discovery verified with a throwaway test that fails when a child file is mis-suffixed. | check: green (196 tests)
P3 S4 | SOURCE apps/admin/src/features/inventory/application/{useInventoryMaterials,useInventoryMovements}.ts + the rules that existed only in components/{InventoryMaterialEditorDialog,InventoryMovementDialog}.tsx as AntD props + the inline low-stock rule in components/InventoryMaterialsTable.tsx + recordMovement/createIngredient/updateIngredient/archiveIngredient and the sort+filter logic from packages/data/src/mocks/InMemoryInventoryRepository.ts → packages/admin-engine/src/engines/inventory/{inventoryEngine.ts,inventoryContracts.ts,children/materials-read/*,children/stock-movements/*,children/stock-adjustment/*} | Inventory part one, landed as three green commits (9b9fad9, 69628a7, 0af1ccf). The port was INVERTED rather than copied: SOURCE's recordMovement computed the delta, mutated the balance, recomputed average cost and appended the ledger row inside the adapter; the target's commitMovement persists a plan that planStockMovement has already validated and computed. That primitive sits in the area contracts, not in stock-adjustment, because LOGIC §8 shows neither S5 child requiring a sibling capability — the one deliberate exception to "contracts hold no behavior", explained at length in the file and flagged as tech-debt D10. Consolidated the low-stock rule from four disagreeing copies into one: SOURCE badged an ingredient with no balance row as low and then HID it when the low-stock filter was ticked, so the filter excluded exactly the rows it appeared to select. Resolved in favour of the badge, with hasBalanceRecord preserving "never counted" vs "empty". Fixed the orphan-row hazard — a refused movement now writes nothing, where SOURCE created a zero balance row before validating and thereby changed that ingredient's future filter behavior. Ordering moved out of the store into both read children with tie-breaks added (ingredient names are not unique; the automated paths stamp every row of one order with an identical timestamp, so the ledger had no total order). Form-only rules moved into logic: quantity minimum, two-decimal rounding, unit compatibility, purchase-requires-cost, no negative cost, no re-archiving. Rounding adopted the domain's EPSILON convention so one input cannot round two ways. Typed results replace SOURCE's single generic toast. Dropped: the outletId that scoped nothing unless lowStockOnly was set, and the hooks' opaque boolean error flag. 8 mutations across the three children — 7 caught immediately; one exposed a test that only checked half of a count relaxation, which was strengthened and re-verified. On-disk discovery verified with a throwaway test, since the structure checker sees a mis-suffixed file only when the new name matches no allowed glob — a rename to *.test.ts passes structure and typecheck while loading as nothing. | check: green (268 tests)
P3 S5 | SOURCE packages/data/src/mocks/InMemoryInventoryRepository.ts {consumeOrder,revertOrderConsumption,calculateHpp,listRecipes} + apps/admin/src/features/inventory/application/{useInventoryHpp.ts,ports/catalogReadPort.ts} + the retry path in features/pos/application/usePosCashier.ts + the refund gate in features/orders/application/commands/cancelOrderCommand.ts + packages/domain/src/inventory/hpp.ts (already ported into finance.ts in P1) → packages/admin-engine/src/engines/inventory/{inventoryContracts.ts (extended),children/stock-consumption/*,children/stock-reversal/*,children/hpp-calculation/*} | Inventory part two, landed as four green commits (47ece49, 65a064f, ba49026, a19ba0a); the area is now complete at six children. Closed tech-debt D14 (referenceId on MovementStoreQuery — SOURCE scanned the whole movement array on every checkout) and half of D16 (listRecipes added; saveRecipe restated as the remaining gap, since LOGIC §8 names no child owning recipe editing). Added commitMovements so a multi-row consumption is one call the caller's atomic boundary can wrap — SOURCE looped single writes, so a failure partway left stock decremented with no way to finish, and its guard then latched on the partial set and returned it forever. Both write children go through S4's planStockMovement rather than reimplementing invariants, which is the payoff for placing it at area level. One interaction S4 could not see: that primitive's 0.01 floor and 2dp rounding are rules about what a PERSON may type, and a consumption quantity is derived from recipe arithmetic that can legitimately be 0.001 — so the input gained QuantitySource and only those two rules branch on it; threading it through the S4 child changed no behavior (268 tests passed unchanged). Fixed: SOURCE's dry run validated LESS than its write (existence only, versus the write also refusing archived ingredients), so an archived ingredient passed the projection and threw mid-loop leaving a half-consumed order — no separate projection exists now; the idempotency guard returned rows silently so POS's retry reported success for an order it never finished (outcome now carries `replayed`); an order with no recipes wrote nothing and never latched the guard, so every retry re-ran it. The sharpest correction is the reversal: SOURCE re-derived from the consumed row's entered quantity and unit against the ingredient's CURRENT definition and never read the stored baseQuantityDelta, so a g→kg edit between consume and cancel restored 1000x and a g→ml edit threw and left the order permanently un-cancellable behind a retryable-but-never-succeeding failure; negating the stored delta is immune to both. hpp-calculation is the first child in the port with a `requires` (admin.menu.catalog-read), so its test is the first to compose two areas and to prove the dependency graph excludes the child when the required area is absent; it also resolves SOURCE's two contradictory resilience policies over one dataset (HPP screen all-or-nothing, dashboard per-item) in favour of degrading, and names the pricing defaults that were library default arguments. Dropped SOURCE's N+1 recipe refetch. New debt: D18 (unpaid consumed order never reversed — slice 8's trigger decision), D19 (triple rounding, pairs with D11), D20 (dashboard COGS restates history when a cost moves — slice 11; fix is to store unitCost on the consumption row, which writes null as SOURCE did), D21, D22. 9 mutations across three children, all caught. Two of my own test assumptions were wrong and the tests caught them: floating-point 220.00000000000003 from SOURCE's own expression, and positional row indexing that ignored the Menu area's ordering promise. | check: green (323 tests)
