# Roadmap — end-to-end porting sequence (durable; survives context loss)

Any session (human or agent) reads THIS file + `plan.json` + `CLAUDE.md` to know
exactly where we are and what comes next. `plan.json` is the authority for exact
file paths; this file is the authority for **order** and **progress**. If they ever
disagree on *where a file goes*, `plan.json` wins.

`plan/tech-debt.md` holds what we knowingly left imperfect and why. Add an entry
there when you preserve something you believe is wrong, or take a shortcut a later
phase must live with — it is what keeps "SOURCE did it this way" from turning into
nobody remembering we noticed.

**Rule for the working agent:** finish a slice → `npm run check` green → commit →
append to `porting-log.md` → tick the box below → STOP and report. One slice per go.

Phase order is locked and each phase is gated on the previous being `done`:
`P1-domain → P2-module-system → P3-admin-engine → P4-storefront-engine → P5-ui-core → P6-apps`

Legend: `[ ]` not started · `[~]` in progress · `[x]` done & checks green

---

## P0 — Guardrail harness  `[x]`
Committed. Baseline. No product code.

## P1 — packages/domain (pure logic, TS stdlib only)  `[x]`
Port from SOURCE domain/business logic. No React, no adapters, no I/O.
- [x] S1 `catalog.ts` — products/menu, pricing primitives
- [x] S2 `orders.ts` — order model + state transitions
- [x] S3 `inventory.ts` — stock consume/reverse (idempotent)
- [x] S4 `finance.ts` — money type, HPP/tax math
- [x] S5 `reporting.ts` — read-only aggregations
- [x] S6 `index.ts` barrel + `domainRules.test.ts` (protected-behavior tests)
- [ ] S7 (optional) domain tidy pass — reduce/clarify code with S6 tests green as the safety net. No behavior change; check must stay green. Skippable.
Gate to done: all P1 `exact` files exist, tests + `npm run check` green.

## P2 — packages/module-system (generic runtime, zero deps)  `[x]`
Engine host + capability wiring. No domain knowledge, no browser globals.
- [x] S1 `operationResult.ts` + `engineContracts.ts` (types first)
- [x] S2 `engineRegistry.ts` + `capabilityRegistry.ts`
- [x] S3 `dependencyGraph.ts` + `discovery.ts`
- [x] S4 `diagnostics.ts`
- [x] S5 `index.ts` + `moduleSystem.test.ts`

## P3 — packages/admin-engine (admin headless logic)  `[x]`
Depends on domain + module-system. MUST NOT import storefront-engine or React.
- [x] S1 contracts + snapshot + `shared/atomicOperationPort.ts`
- [x] S2 `createAdminEngine.ts` + `discoverAdminLogic.ts` + `index.ts`
- [x] S3 menu — `menuEngine.ts`, `menuContracts.ts`, catalog-read, menu-editor,
      variant-management (first real area; sets the pattern — see `p3-handoff.md`)
- [x] S4 inventory A — `inventoryEngine.ts`, `inventoryContracts.ts`,
      materials-read, stock-movements, stock-adjustment
- [x] S5 inventory B — stock-consumption, stock-reversal, hpp-calculation
      (+ `referenceId` filter, `listRecipes`, `commitMovements` on the port)
- [x] S6 finance — `financeEngine.ts`, `financeContracts.ts`, ledger-read,
      transaction-recording, expense-management, refund-projection
- [x] S7 orders A — `ordersEngine.ts`, `ordersContracts.ts`, order-read,
      order-submission
- [x] S8 orders B — order-cancellation + `cancelOrderAtomically`
- [x] S9 pos A — `posEngine.ts`, `posContracts.ts`, session, cart
- [x] S10 pos B — checkout + `submitPosCheckoutAtomically` (the second atomic
      workflow; seven required capabilities, the widest child in the port)
- [x] S11 dashboard — `dashboardEngine.ts`, `dashboardContracts.ts`, overview,
      reports (+ sale-time cost snapshots on inventory consumption movements)
- [x] S12 settings — `settingsEngine.ts`, `settingsContracts.ts`,
      theme-preference, business-hours
- [x] S13 `adminEngineGraph.test.ts` — phase gate (real on-disk discovery; the
      completeness proof the `engines/*` globs cannot give)

## PD — Debt settlement  `[x]`  — complete
The owner decided every open item in `tech-debt.md` on 2026-08-01, then REVISED two
of those decisions the same day once the cost was clear. Read the "Owner decisions"
section at the top of `tech-debt.md` before starting any slice here — it explains the
revision, which matters more than the original decision.

**What changed:** the money rewrite (whole rupiah across the domain) is CANCELLED —
cash was already whole rupiah, and the fractions live only in per-unit cost rates
where they are required. It became a display formatter at P5 instead. And per-item
historical profit needs no domain rewrite: the data is already recorded, so it is a
new read child. **The final two implementation slices did not reopen
`packages/domain`, so nothing here blocked P4.** Debt-first was the owner's
preference, not a dependency.

Every slice below was ADDITIVE or local. No closed phase was reopened.
Slices are lettered (DS-A…) not numbered, because the original DS1-DS8 numbering was
published in a superseded plan and reusing those numbers would silently conflate the
two. Order within the block is a suggestion; only DS-A→DS-C is a hard dependency.

`activePhase` was set to `P3-admin-engine` for this block (the one allowed plan
edit), then returned to `P4-storefront-engine` when it finished. **PD is an ordering
concept in this file only — it is NOT a phase in `plan.json`.**

- [x] DS-A shared rules get a real home: `engines/*/*Operations.ts` (D10).
      **The only item in the whole block needing a `plan.json` line added** — owner
      approves that line, then four functions move out of two contracts files and
      six importers update. Do it FIRST: DS-C touches the same inventory files.
- [x] DS-B `order-progression` child (D28) — the kitchen can advance a ticket again
      (`accepted → preparing → ready → completed`). **First commit amends
      `new-target/LOGIC-TARGET-FILE-TREE.md` §4 and §8** — the owner's authorizing
      act, standing alone and clearly labelled, before any code. Reuse the domain
      transition machine; keep the store's authoritative
      `updated | not-found | invalid-transition` outcome; and NEVER create a general
      status-setter door — S8 deleted exactly that from SOURCE because it accepted
      `"cancelled"` and could bypass the whole atomic cancellation workflow.
- [x] DS-C per-item historical profit (D20) — a new READ child in the INVENTORY
      area joining data that already exists (movement `unitCost` snapshots from S11,
      order items, recipes). Same design-doc-first commit as DS-B. Three constraints
      are non-negotiable and spelled out in D20: the per-dish split is a
      RECONSTRUCTION not a recorded fact and must say so; it is exact only while
      recipes cannot be edited, so revisit at D16; and pre-S11 rows must report
      **unknown**, never zero — zero reads as pure profit, the one wrong answer that
      looks plausible. Dashboard joins this against revenue it already reads and
      must NOT gain repository access (that is D15, resolved at S11).
- [x] DS-D referential integrity (D1 + D2) — each child cleans up the links it
      leaves behind on delete, and saving a menu checks its category exists. Local
      to the menu area, no new files. Watch for existing data with broken links:
      the category check can make previously-saveable rows start failing.

Deferred OUT of this block, deliberately:
- **D16 recipe-editor** is AUTHORIZED but the owner chose to build it with the screen
  that needs it → P5. When it lands, revisit DS-C's constraint 2 (recipe versioning).
- **Money formatter** (replacing the cancelled money rewrite, D11/D19) → P5, listed
  under that phase. Cost values display as whole rupiah; the stored numbers keep
  full precision.
- **D27** is not a decision; it resolves when P4 storefront checkout lands.
- **D8, D22** reverted to `accepted` — both were waiting for a domain reopening that
  is no longer happening.

## P4 — packages/storefront-engine (storefront headless logic)  `[ ]`
Depends on domain + module-system. MUST NOT import admin-engine or React.
- [ ] S1 contracts + snapshot
- [ ] S2 `createStorefrontEngine.ts` + `discoverStorefrontLogic.ts`
- [ ] S3+ one slice per `engines/*` (checkout…), incl. `submitCheckoutSafely.ts`
- [ ] Sn `storefrontEngineGraph.test.ts`

## P5 — packages/ui-core (single shared UI source)  `[ ]`
Headless UI contract layer has NO React/AntD; React/AntD only in `renderer/` +
`layouts/` + `widgets/` + `theme/`. A gate/layout MUST NOT import a repository/engine
internal — only engine public entries.
- [ ] S1 headless: `createUiCore`, `uiContracts`, `uiCoreSnapshot`,
      `layoutRegistry`, `gateRegistry`, `presentationState` + `uiCore.test.ts`
- [ ] S2 7 global layouts (`layouts/*.tsx`) + `globalLayouts.test.tsx`
- [ ] S3 3 widget groups (`widgets/*.tsx`) + `globalWidgets.test.tsx`
- [ ] S4 renderer (`UiHost`, `AntdUiRenderer`, `SurfaceShell`,
      `NavigationRenderer`, `iconRegistry`) + `renderer.test.tsx`
- [ ] S5 theme (`themeContracts`, `createAntdTheme`, `ThemeProvider`) + `styles/uiCore.css`

Carried into P5 from the debt settlement (see `tech-debt.md` "Owner decisions"):
- [ ] **Money formatter** — replaces the CANCELLED whole-rupiah domain rewrite
      (D11/D19). Cash is already whole rupiah; only per-unit cost RATES carry
      fractions, and they must keep them (a spice at 800/kg is 0.8 rupiah/gram).
      So format at the edge: display whole rupiah, store full precision. P5 has no
      allow-globs — every file is `exact` — so this belongs in an existing file,
      most naturally `headless/` since formatting needs no React. Known and
      accepted: a column of small costs can display rows that do not visibly sum
      to their displayed total. Affects displayed COST breakdowns only, never cash.
- [ ] **Recipe editor** (D16) — AUTHORIZED; the owner chose to build it with the
      screen that needs it. Needs a new child in the admin engine, so its first
      commit amends `new-target/LOGIC-TARGET-FILE-TREE.md` §4/§8, exactly as DS-B
      and DS-C do. Port the rules that lived in SOURCE's AntD form props (at least
      one component, quantity min 0.01, cost floors, unit-compatibility filtering)
      into logic. **Do NOT reproduce SOURCE's index-positional component ids**
      (`recipe?.components[index]?.id ?? …`) — deleting a row made the remaining
      rows inherit their neighbours' identities. **When this lands, revisit DS-C
      constraint 2:** editable recipes mean a recipe change silently rewrites what
      past months look like, so recipe versioning must be decided then.

## P6 — apps (admin + storefront shells; gates only)  `[ ]`
Thin app shells that mount ui-core + wire engines. 7 admin gates, 4 storefront gates.
- [ ] S1 apps/admin shell + gates (one gate per sidebar root)
- [ ] S2 apps/storefront shell + gates (one gate per flow)
Gate to done: apps build, all boundary + structure + tests green end to end.

---

## Progress notes
(latest at top — agent appends one line when a slice or phase changes state)
- PD DS-D done — D1 and D2 resolved locally in product commit f234990, with no new file, contract, capability, requirement edge, sibling import, domain edit, `new-target` edit, or S13 edit. The target stores group attachment only in each menu's `variantGroupIds`: deleting a menu removes that menu and therefore its outgoing group links while preserving reusable groups; deleting a group first reads the menus, deletes the group, then attempts to strip its id from every affected menu even after an earlier update returns null or throws. Partial cleanup returns the deleted group id as usable `degraded` data plus one `connection-cleanup-failed` issue per failed menu id. `saveMenu` still re-reads its baseline at save time and now also checks the current category list; a missing nonblank id fails before write as `invalid-input` carrying `category-not-found`. Owner-visible compatibility warning: a legacy menu whose category was already deleted could previously be re-saved, but now refuses save until its category is repaired. Both capabilities remain reachable only through their existing probe-tested children. Two permanent tests were added (545 total); the menu/variant focus is 97/97 and untouched S13 is 21/21. Three mutations each made exactly one selected test red and were restored: category existence removed, group cleanup removed, and cleanup made fail-fast. Full check green: structure 91 files, boundaries, typecheck, 28 test files and 545 tests. The PD block is closed, `activePhase` is back at P4, and P4 has not started.
- PD DS-C done — D20 resolved in two implementation checkpoints plus this bookkeeping: owner authorization first and alone in ca27c32 (`new-target` §4 adds `historical-item-profit`; §8 grants exact capability `admin.inventory.historical-item-profit` requiring `admin.orders.read`), then product in f147482. The Inventory child joins sale-time movement cost snapshots, Orders items/revenue, and current recipes without giving Dashboard repository access or reopening `packages/domain`. It independently refilters outlet, Jakarta dates, consumption type/reference, and optional menu id; deterministic ordering breaks equal movement/order timestamps by id and duplicate menu names by menu id. Shared-ingredient costs are divided by recipe consumption proportion, packaging/extras retain full precision, and the aggregate carries explicit `recipe-proportional-reconstruction` plus `current-recipe-assumed-unchanged` metadata. A recipe/recorded-quantity mismatch degrades and D16 now records the required versioning revisit. Most importantly, any included pre-S11 `unitCost: null` makes the affected menu's cost/profit `unknown` with null amounts—never zero and never a misleading known partial subtotal. Movements/recipes may fail independently and degrade while Orders remains usable; all-source failure is failure, and Orders failure never invents an empty result. Missing Inventory port keeps the child active/published with one diagnostic and normalized call failures. Capture and reconstruction now share `consumptionQuantity` through `inventoryOperations.ts`. Fifteen permanent tests reach the capability through a probe; S13 proves 25 children, seven requirement-bearing children, and the exact literal id/read edge. Three mutations each failed exactly one focused test and were restored to SHA-256 C102E6EC5E6EB062807A399A6F48FEC524BAAFF66D152B1B40025649FBE9321E: null→zero, proportional→equal split, and removal of the duplicate-name id tie-break. Full check green: structure 91 files, boundaries, typecheck, 28 test files and 543 tests. Stopped before DS-D.
- PD DS-B done — D28 resolved in two implementation checkpoints plus this bookkeeping: owner authorization first and alone in 3b12f3b (`new-target` §4 adds the two planned files; §8 adds exact capability `admin.orders.order-progression` requiring `admin.orders.read`), then product in 1493d9f. The new child restores SOURCE's `new → accepted → preparing → ready → completed` workflow through one narrow `progressOrder` port whose target type contains only `accepted | preparing | ready | completed`; neither `new` nor `cancelled` can be requested structurally, and an explicit runtime guard rejects `cancelled` before the port because the closed domain machine itself permits cancellation/refund. There is no general status setter and no pre-read: the store remains the sole authoritative judge and returns `updated | not-found | invalid-transition`, while the injected adapter/store owns the domain transition, clock, event id, and commit. The read requirement is a liveness boundary—progression is excluded when Orders read is down—not a decorative call, confirming D29's definition of `requires`. A missing store keeps the child active/published with one diagnostic and normalized call failures. Tests reach the capability only through a probe child; the S13 literal gate now finds 24 children, maps the exact same capability id, and enforces the new read edge. Three mutations each failed exactly one focused test and were restored to the same SHA-256: allowing `cancelled` reached the domain-backed store and wrongly returned success, treating invalid transition as success was caught, and leaking a store exception was caught. Full check green: structure 89 files, boundaries, typecheck, 27 test files and 528 tests. Stopped before DS-C.
- PD DS-A done — D10 resolved. The owner-approved structural slot landed first and alone in 53fbba3: `activePhase` moved to P3 and both `engines/*/*Operations.ts` plus `engines/*/*Operations.test.ts` globs were added, with no other plan field changed. Commit `refactor(admin-engine): DS-A move shared rules to operations` then moves exactly four existing functions—Inventory's `roundEntered`, `recomputeAverageUnitCost`, and `planStockMovement`, plus POS's `posCartFingerprint`—with their comments and bodies byte-identical into `inventoryOperations.ts` and `posOperations.ts`; all listed callers now import the area operation rather than a contracts file. No test or expectation changed, no child was added, and the S13 graph gate still proves all 23 children. Every area contracts file now exports zero functions, Operations imports Contracts in one direction only, and the new files are accepted by the two owner-approved globs. D10's live entry was deleted per the register rule and its story moved to the porting log. Full check green: structure 87 files, boundaries, typecheck, 26 test files and exactly 516 tests. Stopped before DS-B.
- **P3 DONE** — phase closed, status flipped in plan.json, `activePhase` moved to P4-storefront-engine. All 9 planned admin-engine `exact` files plus 7 area engines, 7 contracts files, 23 children, 23 child tests and 2 atomic workflow files exist; 516 tests green across 26 files. With the phase marked done the structure checker now enforces every `exact` P3 file, and it passes.
- P3 S13 done — `adminEngineGraph.test.ts`, the phase gate, in one green commit (be938e9). **This closes P3.** The slice ported no behavior: it is the completeness and composition proof that `plan.json` lists as `exact` precisely because the `engines/*` entries are `allow` globs, so a partial or entirely missing area passes all four checks. Every other test in the package injects its definitions, which proves a child behaves correctly when handed to a runtime but can never prove the child is REACHABLE in a real build; this is the only test that composes from real on-disk `import.meta.glob` discovery with nothing injected. It proves: discovery finds all 7 engines and all 23 children; every capability id matches LOGIC §8 verbatim, including the two that deliberately differ from their child id (`admin.orders.cancel`, `admin.orders.read`); the five children with requirements declare exactly the doc's edges in the doc's order while the other eighteen require nothing; the injected atomic port is republished as `admin.atomic-operation` and, when absent, exactly the two atomic children are excluded while their siblings keep running; a healthy runtime reports ZERO diagnostics; providers initialize before consumers; and disposal is idempotent, leaving zero active children with no failures. Two deliberate design choices drive the whole file. First, ids are asserted as SETS, not counts — a renamed file keeps the count identical while loading as nothing, and "expected 23, got 22" names no culprit, whereas a set diff names the missing child. Second, every expectation is a LITERAL string rather than an import from the area contracts: importing `ORDER_CANCEL_ID` would make the test agree with the code by construction, so a rename would rename the expectation with it and the test could never fail. Three mutations, all caught and restored byte-for-byte. The decisive one closes the blind spot nine prior slices proved with a throwaway test that was then deleted: `businessHoursChild.ts` was renamed to an allowed `*.test.ts` glob AND its sibling's import was fixed so the mutation was completely silent — structure stayed GREEN at 85 files "where the plan says", and all 497 other tests passed, including the business-hours suite happily testing a child the real runtime never loads. Only this gate failed, naming `admin.settings.business-hours` in the diff. The other two: dropping hpp-calculation's LOGIC-required catalog-read edge (3 failures) and publishing cancellation under its child id instead of `admin.orders.cancel` (4 failures, which is what proves the literal-strings decision). One assumption was corrected rather than asserted away — a graph-EXCLUDED child reports `unmetRequirements: []`, because the registry fills that field only for children it actually tried to initialize; the reason lives in the graph's `missing-dependency` diagnostic instead, so the gate asserts it there and documents why an empty array is not "no reason known". Tech-debt housekeeping: resolved D18 and D23 DELETED from the register per its own "delete on the way out" rule (their stories live in the S8/S10 notes above); D10, D16, D28 and D29 judged with the complete graph in hand. **Three items now need an owner decision and none can be actioned by an agent, because all three would require adding lines to `plan.json` (rule 7): D28** (no child owns forward order progression — an operator can read and cancel orders but cannot move one from accepted to preparing to ready to completed; LOGIC §4/§8 has no slot for it, so this is the target tree's own gap, and the gate now enforces it by making a quiet widening of `order-read` fail), **D16** (no recipe write path — confirmed unchanged now that all seven areas exist), and **D10** (the shared write primitive in a contracts file — the deferred evidence is in and says it is a pattern, not an inventory quirk: inventory has three such functions and POS has `posCartFingerprint`, while the other five areas hold types only). D29 stays accepted and its parked question is answered by the gate itself: `requires` states what must be RUNNING for a child to be safe to publish, not what it calls. Checks green, 516 tests.
- P3 S12 done — Settings theme-preference + business-hours, in two green commits (d2e5adf theme preference, de42ceb business hours). **All seven operational admin areas are now complete; P3 is 12 of 13 slices, and only the S13 `adminEngineGraph.test.ts` phase gate remains.** The parent is identity-only, both exact LOGIC/UI capability ids publish with `requires: []`, and neither child imports the other. Theme preference is headless schema-v2 state behind an injected transport-neutral persistence port: it validates and normalizes the SOURCE preference model, migrates schema v1 in memory with a degraded issue, defaults safely on empty or malformed persisted input, validates before one write, and never imports React, AntD, CSS, or browser globals. Business hours is backend-owned operational state behind its own injected port, keyed explicitly by outlet id without claiming ownership of the active outlet; it validates complete weekday schedules, ranges, special schedules, real dates and overlap rules before one write, normalizes range order, and fixes all calendar evaluation to Asia/Jakarta. Its pure availability policy combines outlet hours with an optional Menu `SalesSchedule`, closing S9's missing-policy gap without wiring POS in S12: a later owner-approved graph/composition change must pass outlet id, checkout instant, and the menu schedule after catalog revalidation and before the first checkout write. No D30 was opened because the policy now exists; the integration edge remains intentionally undecided. D25 remains open because Settings keys data by outlet but does not own outlet selection, and no outlet-management child was invented. A missing port leaves each child active and published, emits one diagnostic, and makes every call return a normalized dependency failure. Four load-bearing mutations were each caught by exactly one focused test. A real-discovery witness passed with both children, failed when the business-hours production file was renamed to an allowed `*.test.ts` while structure stayed green, and passed again after byte-for-byte restoration. Checks green, 495 tests.
- P3 S11 done — Dashboard overview + reports, in two green commits (8a576d9 overview + sale-time consumption-cost snapshots, 97234d9 reports). **The Dashboard area is complete at two children; P3 is now 11 of 13 slices, and S12 settings is next.** Both children declare the LOGIC §8 capability graph verbatim: overview requires Orders read, Inventory materials-read, and Finance ledger-read; reports requires Orders read, Inventory stock-movements, and Finance ledger-read. They consume only those published capabilities—never an inventory repository/store shape—so D15 is resolved, archived materials stay out of overview, and the stock report reconstructs balances from the movement ledger. Each source degrades independently, preserving usable data and upstream issues; when every source is unusable the call fails instead of fabricating an empty dashboard. Jakarta owns calendar filtering, and the domain reporting module remains the sole aggregation owner. D20 is narrowed but deliberately remains open: new consumption movements snapshot sale-time cost in their entered unit (including unit conversion), and legacy null-cost rows make reports explicitly degraded; however, the closed domain reporting API still accepts current `menuHpp`, while movements identify only an order rather than an order item/menu. The exact Dashboard graph also has no catalog/HPP edge, so S11 neither smuggles in a hidden dependency nor restates history with today's HPP; historical item-level COGS needs a later domain/attribution decision. D11/D19 precision behavior was not changed. Four load-bearing mutations were each caught by exactly one focused test, and a real-discovery witness failed under an allowed-glob rename while structure stayed green, then passed after byte-for-byte restoration. Checks green, 471 tests.
- P3 S10 done — POS checkout + `submitPosCheckoutAtomically`, in three green commits (ffc2132 contracts, 68e1606 child + atomic workflow, 1af89fd the D23 guard test). **The POS area is complete at three children; P3 is now 10 of 13 slices, and S11-S12 (dashboard, settings) are next.** This is the second and last atomic workflow in the port and the widest child in it: seven required capabilities (session, cart, catalog, stock-consumption, order-submission, transaction-recording, atomic-operation), each proven to make the child unavailable when absent rather than to fail at call time. **Tech-debt D23 is resolved, and the strong default recorded in S6 held**: the sale stays DERIVED. Checkout declares and resolves the finance capability exactly as LOGIC §8 requires — so the graph edge is honest — but never calls the manual writer, leaving one writer for one fact and no de-duplication to keep load-bearing. Instead it VERIFIES the derivation before finalizing the till: it projects the committed order and demands exactly one row of type `sale`, rolling everything back otherwise. That guard is not defensive padding, and the reason is the sharpest thing this slice found: checkout stamps `paid`, but the store returns the STORED order, and on an idempotent replay that order may have been cancelled since it was written — the domain settles paid→refunded on cancellation, so a refunded order projects TWO rows and deriving the till's sale from it would book a refund the drawer never paid out. The same mechanical rule as S8 governs the endings, and S8's note predicted it exactly: inside an atomic boundary a RETURNED failure commits, because rollback is driven by a throw. So every pre-write refusal (empty cart, unavailable menu, insufficient cash, closed session, cart-changed-during-identity-reservation) returns and writes nothing, while every failure after the first write throws `PosCheckoutRollback` and undoes all owners. **S9's carried-forward warning was real and is closed**: nearest-step rounding produces a NEGATIVE rounding adjustment (−49 on the worked example) while order-submission's validator rejects every negative Money; S10a resolved it by narrowing that validator to exempt `totals.rounding` alone, so S9's subtotal behavior was not distorted to satisfy a validator. Cart finalization is revision- and fingerprint-guarded, so items added between submission and clear are preserved instead of silently destroyed, and the cash-sale increment rides the same call. Order and inventory replays are reported as degraded rather than collapsed into success or failure. 17 permanent tests. The mutation round is the reason this slice took a third commit: deleting the D23 guard left all 16 tests passing, because the only finance assertion was "the manual writer was not called" — which proves there is no second writer but says nothing about the row the till relies on. My first replacement test was itself worthless (it called the domain projection directly, where checkout's own `paid` stamp makes it pass with or without the guard) and it did not compile; it was rewritten to drive the guard through the real runtime and then verified able to fail — disabling the guard fails that test and only that test. Checks green, 450 tests.
- P3 S9 done — POS session + cart, in two green commits (93ec301 engine+contracts+session, 0f4ef83 cart). **P3 is now 9 of 13 slices; S10 checkout is next and is the widest child in the port at seven required capabilities.** Note on bookkeeping: S9's code landed at 00:32 but its roadmap tick, this note, and its porting-log line were written afterwards by a later session, because the building session recorded its research in p3-handoff.md and stopped before updating the plan-of-record. The handoff's S9 section is kept with a status correction at the top rather than rewritten, since its SOURCE research is sound and its S10 findings are load-bearing. Session preserves SOURCE's single open/closed till for one outlet, whole non-negative IDR opening balance, expected cash as opening + cash sales, and a SIGNED close variance — but closes the hole where a second open silently reset an active till and lost the prior session, which in SOURCE was prevented only by a hidden button; it is now a named conflict, and that guard is mutation-tested. Session deliberately does NOT clear the cart on close even though SOURCE did: LOGIC §8 gives session no cart requirement and cross-child imports are forbidden, so cart exposes clear and the caller that owns the close flow composes both capabilities. Cart owns line snapshots, quantity, variant configuration, clear, and the unit/line/subtotal projections only; checkout defaults, tax, rounding, cash sufficiency, receipt, order number and aggregate construction all stay in S10. Merge identity is menu id + sorted (groupId, optionId) pairs + trimmed note, so variant input order and outer whitespace do not create duplicate lines while case and internal whitespace still do. Persistence is an injected transport-neutral port rather than SOURCE's single unrestricted `update(updater)` mirrored to a shallowly-validated sessionStorage blob; a missing port follows the established rule (child stays active, publishes, reports one diagnostic, every call returns a normalized dependency failure) and browser storage is never imported. Availability reuses the domain's `isMenuAvailable` combined with visibility rather than SOURCE's literal status check, making S9 the second strict-ordering reader and consolidating the D6 family; schedule enforcement is deliberately NOT invented here and waits for S12 to supply the policy. Verified later: on-disk discovery finds the engine and both children with LOGIC §8 ids verbatim and no `requires`, and the witness was proven able to fail — an allowed-glob rename left structure green while the cart child loaded as nothing. Five other behaviors have tests but were not mutation-checked. **Carried to S10: nearest-step rounding can produce a NEGATIVE rounding adjustment, while order-submission's validator rejects every negative Money including `totals.rounding` — confirmed in the code, and S10 must resolve it rather than distorting S9's subtotal behavior.** Checks green, 430 tests.
- P3 S8 done — order cancellation, in two green commits (b4c5b0c contracts, 6455f64 child + atomic workflow). **The Orders area is complete at three children; P3 is now 8 of 13 slices.** This is the first real consumer of the atomic seam built in S1 — until now that republish was declared and never exercised. **Tech-debt D18 is resolved by owner decision (2026-07-31): return stock whenever it was actually deducted.** SOURCE decided that by asking the FINANCE projection — `projectRefund(order).length > 0` — which reduces to "was this paid", because the domain only settles `paid → refunded` on cancellation. A money fact was answering a stock question, and the consequence was silent: an unpaid order that had consumed stock was cancelled and never got it back, so inventory drifted permanently low, which is what raises false low-stock warnings and drives over-ordering. The reversal is now attempted on every cancellation and the reversal capability — the only owner that authoritatively knows whether this order consumed anything — is the sole judge; the refund projection still runs and reports but decides nothing. The mechanical fact that shaped the whole design: inside an atomic boundary a RETURNED failure commits, because rollback is driven by a throw. So the endings are strictly separated — a refusal that wrote nothing is returned, and any failure after the write throws and rolls back all three owners. Both directions are mutation-tested, and slice 10's POS checkout faces exactly the same split. Calling the reversal unconditionally created its own trap: `stock-reversal` reports "never consumed" as a failure, which is right for its own caller but is an ordinary ending here, so its endings had to be classified (never-consumed and already-reversed benign, everything else fatal) — treating them as fatal would have made most orders impossible to cancel. Removed SOURCE's `updateStatus(orderId, status)` bypass, which accepted `"cancelled"`, wired straight to the repository, and could flip a paid order to cancelled/refunded with no reversal outside any transaction — its own comment claimed `cancel` was the single cancellation command while that sibling contradicted it, and the invariant survived only because one screen filtered the value out of a button list. Four endings a caller acts on are now distinct (stock returned, stock already returned, refund owed, already-cancelled vs not-cancellable) where SOURCE had one boolean meaning "was paid" displayed as though it meant "stock came back". The capability id is `admin.orders.cancel` per LOGIC §8, not the child id — the one place in the area they differ, and publishing under the wrong id failed 16 of 20 tests. New debt D29 (`admin.orders.read` is declared per LOGIC and deliberately never called; the store is the authoritative judge and a pre-read would be a second judge and a race). 7 mutations, all caught; typecheck caught two fixture defects the tests could not see. Discovery witness re-confirmed: structure stayed green under an allowed-glob rename while the child loaded as nothing. Checks green, 412 tests.
- P3 S7 done — orders read + submission, in four green commits (53a9ee1 contracts+engine, b19cdcc order-read, 5b06da9 order-submission, 764d885 exact-id correction). **P3 is now 7 of 13 slices; S8 cancellation is next and remains completely separate.** Order-read owns SOURCE's search across order number/customer/item, exact status/outlet/channel filters, and newest-first order; it adds a deterministic id tie-break and validates date grammar/range before I/O. SOURCE filtered calendar dates as UTC while displaying in machine-local time; Orders now joins Finance/reporting on Jakarta, protecting the near-midnight case. The port promises raw unordered rows and an authoritative exact-id lookup; the child owns all filtering/order and maps invalid input, not-found, missing port, and backend failure distinctly. Order-submission stays a narrow complete-order handoff — no POS/cart/pricing/inventory/finance/cancellation behavior moved into it — but closes SOURCE's duplicate-on-retry hole with a required idempotency key and authoritative `created | replayed | conflict` store result. Replay is observable, conflict is not collapsed into infrastructure failure, and complete shared invariants are decided before one write. Cancellation and normal forward status transitions were not smuggled into either child; their ownership remains explicit for later work. Six deliberate mutations were caught and restored. Temporary Vite discovery found the parent + both children; renaming submission to an allowed `*.test.ts` kept structure green while discovery failed, proving the witness. Checks green, 392 tests.
- P3 S6 done — finance, in six green commits (6d603b5 contracts+engine, 40d2308 refund-projection, 9b91302 ledger-read, 0dc6370 transaction-recording, 77ffed8 expense-management, 9f67f13 integrity corrections). **The finance area is complete: four children; P3 is now 6 of 13 slices.** The structural fact that shaped everything is that SOURCE stored only MANUAL transactions — sales and refunds were derived from orders at read time with deterministic ids. Preserved deliberately: no persisted sale/refund, no second writer, and refund-projection is pure. Ledger-read receives manual rows and orders through separate injected ports because LOGIC §8 gives it no required capability; it keeps either healthy dataset when the other fails, replacing SOURCE's one-Promise.all/one-catch policy that blanked all rows. The final integrity pass caught the boundary case: when EVERY configured source fails, fallback [] is fabricated rather than usable and now returns failure. Resolved the area's duplicate rule: SOURCE made presets in machine-local time, interpreted date-only filters as UTC, and reporting used Jakarta; both preset and filtering now use the domain's Jakarta day, with the near-midnight case protected. Transaction recording moved all form-only rules below the screen (whole non-negative IDR, image/PDF ≤5 MB, three length caps, category-direction compatibility, named direction→type policy), closes the unknown-category hole the domain's lookup missed, and uses authoritative tagged write outcomes rather than a pre-read: update distinguishes missing/automatic/voided, and idempotent void says whether it actually changed anything. Dead SOURCE port surface removed before it fossilized: production-unused get-by-id, exposed id generator, and store-side query; a restated domain type became an alias. Expense-management is deliberately thin because SOURCE's screen merely forced outflow and added a breakdown; it has no sibling import/requirement and projects posted outflows from rows a caller already read, fixing the visible mismatch where pending/voided rows appeared but did not contribute to totals. Refund projection's contract states the slice-8 trap: refundable means money, never whether stock needs reversal; D18 remains for cancellation. New debt: D23 (slice 10 must reconcile POS's required finance call with a derived sale — SOURCE made no call), D24 (three domain transaction types unreachable), D25 (default outlet `wm-1`), D26 (custom-category slug collisions). 7 deliberate mutations across four children, all caught and restored byte-for-byte. On-disk discovery re-verified: structure stayed green when refundProjectionChild was renamed to an allowed `*.test.ts`, while real discovery failed; restored and throwaway deleted. Public-boundary Vite runtime observation found 4/4 finance children active, honest missing-port diagnostics, and idempotent disposal leaving zero active with no failures. Checks green, 371 tests.
- P3 S5 done — inventory part two, in four green commits (47ece49 contract extension, 65a064f stock-consumption, ba49026 stock-reversal, a19ba0a hpp-calculation). **The inventory area is complete: six children.** Two tech-debt items closed (D14 `referenceId` filter, D16 half — `listRecipes` landed, `saveRecipe` restated as the remaining gap). The slice's structural payoff was S4's decision to put `planStockMovement` at area level: all three write children now share one set of invariants, so a consumption cannot disagree with a manual adjustment about what a legal stock change is, and neither S5 child needed to reimplement anything. One interaction S4 could not have seen: that primitive's 0.01 floor and two-decimal rounding came out of the manual entry FORM, and a consumption quantity is computed from recipe arithmetic (`component.quantity × item.quantity × (1 + waste/100)`) which can legitimately be 0.001 or carry many decimals — so applying typed-entry rules to it would have refused orders SOURCE accepted. Rather than fork the primitive, the input now carries `QuantitySource` and only those two rules branch on it; every other invariant applies to both, which is the reason there is one primitive at all. Threading it through the S4 child changed no behavior (all 268 tests passed unchanged). Defects fixed, each mutation-tested: SOURCE's consumption ran a dry run that checked LESS than its real write (existence only, versus the write also refusing archived ingredients), so an order naming an archived ingredient passed the projection and threw partway through the write loop leaving stock half-consumed — there is now no separate projection to disagree with; SOURCE's idempotency guard returned existing rows silently, so the POS retry path cleared its pending-sync flag for an order it had never finished, and the outcome now carries `replayed`; an order where nothing has a recipe wrote zero rows and, because the guard keys on rows existing, never latched, so every retry re-ran it — now a named failure. The reversal correction is the sharpest: SOURCE rebuilt each reversal from the consumed row's ENTERED quantity and unit and re-ran the conversion against the ingredient's CURRENT definition, never reading the delta it had recorded — so editing an ingredient from g to kg between consuming and cancelling restored 1000× what was taken, and editing g to ml made the conversion throw, which through cancellation's rollback left the order permanently un-cancellable with a retry that could never succeed. Negating the stored `baseQuantityDelta` is immune to both, and both scenarios are tested. hpp-calculation is the first child in the whole port with a `requires` (LOGIC §8 gives it `admin.menu.catalog-read`), so its test is the first to compose TWO areas — the cross-area wiring is now proven rather than declared, including the case where the Menu area is absent and the dependency graph correctly excludes the child instead of letting it publish a broken capability. It also fixes the two contradictory resilience policies SOURCE had over one dataset: the HPP screen blanked every row when any single menu threw while the dashboard degraded per item; the degrading one is now the only one. Pricing defaults (60% target margin, round to 500) were library default arguments at one call site and are now named constants. A test caught my own wrong assumption — assertions indexed rows positionally, but they arrive in the Menu area's own order, which is its promise to keep and not this child's to assume. New debt recorded: D18 (an unpaid consumed order never gets stock back — belongs to slice 8's trigger decision, not to inventory), D19 (HPP rounds three times; pairs with D11), D20 (dashboard COGS restates history whenever a cost moves — slice 11, and the fix is to store unit cost on the consumption row, which currently writes null as SOURCE did), D21 and D22 (both accepted, both structural consequences of the domain being a closed phase). 9 mutations across the three children, all caught. Discovery re-verified: structure passed while the child loaded as nothing, exactly as the corrected S4 note predicts. Checks green, 323 tests.
- P3 S4 done — inventory part one, in three green commits (9b9fad9 contracts + engine + materials-read, 69628a7 stock-movements, 0af1ccf stock-adjustment). The area-slice pattern held; the one structural decision worth carrying forward is where the shared write primitive lives. SOURCE's `recordMovement` was a single store method that computed the balance delta, mutated the balance, recomputed the ingredient's average cost and only then appended the ledger row — arithmetic in the adapter, three writes in an order the caller could not see. The target inverts that: `planStockMovement` validates and computes everything and touches nothing, and the store's `commitMovement` persists the finished plan. It sits in `inventoryContracts.ts` rather than in `stock-adjustment`, which is the one deliberate exception to "a contracts file holds no behavior" — LOGIC §8 shows neither S5 child requiring a sibling capability, so consumption cannot depend on the adjustment child, and leaving the invariants there would force S5 to reimplement them. That is exactly how SOURCE ended up with four copies of its low-stock rule. `plan.json` has no shared-helper slot inside an area and `packages/domain` is a closed phase, so the choice was between this and duplication; it is tech-debt D10 and the owner can decide whether a slot belongs in the plan before S5 builds on it. The headline defect fixed: SOURCE had four live low-stock implementations plus an unused one in the domain, disagreeing about an ingredient with no balance row — the table badged it low, the store's filter could not return it, the dashboard excluded it, the usage report included it — so ticking "low stock only" HID rows the same list had just badged. One rule now, resolved in favour of the badge, with `hasBalanceRecord` keeping "never counted" distinguishable from "empty". Second real fix: a refused movement now writes nothing, where SOURCE pushed a zero-quantity balance row for a new (ingredient, outlet) pair BEFORE applying the delta, so a movement rejected for insufficient stock permanently created a row that had not existed and thereby changed how that ingredient answered the low-stock filter afterwards — a failed write altering query results. Ordering moved out of the store into both read children, with tie-breaks added in both: ingredient names are not unique, and the automated paths stamp every row of one order with an identical timestamp, so the ledger had no total order and could present the same audit history differently on two machines. Form-only rules moved into logic per LOGIC §13: quantity minimum 0.01 (the domain accepts zero, so logic was writing no-op ledger rows), two-decimal rounding, unit-must-convert (SOURCE only pre-filtered the dropdown, so every non-UI caller skipped it), purchase-requires-cost (SOURCE coerced a missing cost to zero, dragging the weighted average down), no negative cost, no re-archiving. Rounding adopted the domain's EPSILON convention after a test caught 1.005 rounding down — two places in one runtime must not round one input differently. Typed results replace the single generic toast that made a negative-balance rejection and a dead backend indistinguishable. Dropped: the `outletId` that scoped nothing unless `lowStockOnly` was set, and the hooks' opaque boolean error flag. 8 mutations across the three children; 7 caught immediately, and the eighth exposed a test that asserted only half of a count relaxation — strengthened, then re-verified. Also corrected a claim in the handoff: the structure checker DOES catch a mis-suffixed child when the new name matches no allowed glob; what it is blind to is a rename whose new name matches a different allowed glob (`materialsReadHelper.test.ts` passes structure and typecheck while loading as nothing). The throwaway discovery test remains necessary, for a narrower reason than recorded. Checks green, 268 tests.
- P3 S3 done — menu, the FIRST real operational area, in three green commits (69b560d scaffold + catalog-read, d14ffb5 menu-editor, 8513852 variant-management). Landing an area as several commits is legal and now the pattern: `plan.json` lists `engines/*` as `allow` globs rather than `exact`, so a partial area passes all four checks, and completeness is slice 13's phase-gate test. Each commit was green before it landed, so each is a fallback point. Structure: `menuEngine.ts` (identity only, no child imports), `menuContracts.ts` (capability tokens, the ONE outbound catalog-store port, and the query/editor shapes that are admin concerns rather than domain vocabulary), three children each resolving the port themselves — forced, since LOGIC §8 shows no menu child requiring anything, so menu-editor cannot depend on catalog-read. Six deliberate departures from SOURCE, all recorded in p3-handoff: ordering (sortOrder, then name) moved out of the in-memory repository into the read child, because in SOURCE a different adapter would have silently changed menu order; the variant availability filter compares against the requested value instead of the literal "unavailable" SOURCE hardcoded; field limits that existed only as form `maxLength` attributes moved into logic per LOGIC §13, along with "a group keeps at least one option" which was only a disabled button; the save baseline is re-read at save time instead of captured at mount, closing the window where a slug or compareAtPrice changed elsewhere gets overwritten; the scheduled-menu interval id comes from the store's generator rather than the literal "sales-interval-default" that let two menus edited in one session collide; and a variant group's non-atomic two-phase save now attempts every menu and reports `degraded` naming the failed ones, where SOURCE threw on the first and left the rest unattempted behind one generic error. It stays non-atomic on purpose — LOGIC §10 scopes the atomic port to order cancellation and POS checkout. Fixed a real invariant hole: the inline option delete wrote the filtered array back without consulting the selection rule, so it could leave a group demanding more selections than it had options; turning an option OFF is guarded for the same reason, since the rule is judged against AVAILABLE options. Left alone deliberately and written down: deleting a menu or group strips no `variantGroupIds`, so dangling links survive and only POS notices at read time. Dropped: the dead `MenuListQuery` store-side search, the module-level repository Proxy singleton the port replaces, the React hooks' uncached state, and a never-reachable discount-price display branch. `DeleteMenuCategoryResult` folded into the one `OperationResult` shape (in-use is a conflict carrying the count). Tests reach each capability through a probe child that declares it in `requires`, the path a real consumer uses, so a capability published under the wrong id fails the suite. 18 mutations across the three children, all caught. Also verified on-disk discovery with a throwaway test — the structure checker catches a misplaced file but NOT a mis-suffixed one, and a renamed child file produced 3 failures before being restored and the check deleted. Checks green, 196 tests.
- P3 S2 done — createAdminEngine.ts (the LOGIC §7 startup sequence: discover → graph → register → initialize → expose; owns the two jobs the generic runtime leaves to a host, namely fan-in diagnostic de-duplication and republishing the injected atomic port as the capability LOGIC §8 requires) + discoverAdminLogic.ts (the two §7 globs, scoped to this package's own engines/ folder so "Admin never scans Storefront" is structurally true, plus a namespace filter as defence in depth) + index.ts (hand-picked public exports; discovery deliberately NOT exported — the legitimate form of that need is the typed `definitions` option). Settled two mechanics by experiment rather than assumption: `import.meta.glob` must be written out literally (aliasing it type-checks and then throws at runtime — the variant that passed tsc most easily would have shipped broken) and needs a narrow ImportMeta type augmentation to pass tsc; a pattern matching zero files returns {} rather than throwing, which is why an empty engines/ folder starts cleanly. Verified 19 behaviors with a temporary suite, then verified on-disk discovery end to end with a scratch area (removed — it is S3's work). Mutation round caught a real defect: the first de-duplication test was VACUOUS — a graph-excluded child is never initialized, so only one diagnostic ever existed and the assertion passed with dedupe switched off. Rewrote it around a genuinely reachable duplicate (a throwing provider, whose consumer does reach the registry) and it now fails when dedupe is disabled. The same investigation exposed a second defect: the missing-atomic-port diagnostic fired even when nothing required an atomic boundary, putting an error in a healthy runtime's report; it is now conditional on a child actually declaring the requirement. All five mutations caught. Checks green, committed d32dba3.
- P3 S1 done — admin-engine scaffold + adminEngineContracts.ts (runtime identity, ADMIN_AREAS expectation list, composition options, narrowed runtime handle that cannot register after startup) + adminEngineSnapshot.ts (pure projection of the generic snapshot into per-area state; disposed children counted as neither failed nor unavailable; unexpected areas appended, never dropped) + shared/atomicOperationPort.ts (both ends of the atomic seam in one file: the inbound outbound-port token composition supplies, and the outbound capability token children require per LOGIC §8). Resolves the open design question flagged before the phase: the atomic operation arrives as an injected port and the engine root republishes it as the capability — no module-system change needed, P2 stays closed. Phase 3 activated. Verified 14 behaviors with a temporary suite (deleted; permanent tests ship per-child from S3 on, plus the phase-gate test at Sn). Mutation-checked the two load-bearing tests (counting disposed as failed; dropping an unexpected area) — both caught. Checks green, committed 0e70323.
- **P2 DONE** — phase closed, status flipped in plan.json. All 11 planned module-system files exist; 51 permanent tests + 17 domain tests green.
- P2 S5 done — index.ts (hand-picked public exports, matching SOURCE's barrel style; capabilityRegistry deliberately NOT exported — it is the staging seam the engine registry owns, and a host publishing behind its back would break staged rollback) + moduleSystem.test.ts (51 permanent tests consolidated from SOURCE's 5 suites, written against the public barrel so a missing export fails the suite). Dropped SOURCE's moduleSurfaceBoundary suite: it re-implemented an import scanner inside a test, and `npm run check` already enforces that rule for every package — a second owner is drift. Finished the S4 severity correction: the leftover hardcoded "warning" in engineRegistry is gone and all three files now derive severity from the one map, so no report site can state its own. Mutation-checked the two load-bearing tests (softened a code to warning; leaked the snapshot array) — both caught. Typecheck also caught a readonly-cast the tests could not. Checks green, committed 677ae6f.
- P2 S4 done — diagnostics.ts (collector with grouping by child/engine, severity filter, summary with fatal signal, optional de-duplication). Ported SOURCE's collector plus the dedupe filter SOURCE had inlined in createModuleRegistry. Registry now collects through it (dedupe off — its report sites are already distinct; fan-in dedupe belongs to the P3 host). Corrected my own invention mid-slice: I had made missing-dependency a warning, but SOURCE marks every code an error and "may we continue" is already carried by the degraded status — two owners for one fact is drift, so severity stays uniform. Verified 17 behaviors with a temporary suite (deleted; permanent tests are S5). Checks green, committed 8f76975.
- P0 done — harness committed, all checks green on empty repo.
- P1 S1 done — catalog.ts ported (types + validation + variant rules consolidated), domain scaffold added, checks green, committed 0800e8f.
- P1 S2 done — orders.ts ported (types + status transitions consolidated), imports Money from ./catalog, checks green.
- P1 S3 done — inventory.ts ported (types + unit conversion + stock math). HPP *functions* deferred to finance.ts (S4) per roadmap; recipe/HPP types kept here. Domain stock primitives only; idempotent consume/reverse belongs to admin-engine (P3). Checks green.
- P1 S4 done — finance.ts ported (types + validation + ledger + calculations consolidated) plus HPP functions from inventory/hpp.ts. Imports Money/Order/inventory recipe types. Checks green.
- P1 S5 done — reporting.ts ported (types + dashboard + reports consolidated). Read-only aggregations over ReportingSnapshot; imports from catalog/finance/inventory/orders. Checks green.
- P2 S3 done — dependencyGraph.ts (startup order + duplicate/orphan/missing/cycle exclusion, deterministic) + discovery.ts (quarantines unknown candidates so nothing malformed reaches the registry). Edges derived from requires→provides since the target has no dependsOn. Dropped SOURCE's UI-field and surface validation. Verified 12 behaviors with a temporary suite (deleted; permanent tests are S5). Checks green, committed 4a02ae4.
- P2 S2 done — capabilityRegistry.ts (staged scopes: a child's capabilities stay pending until it is fully created) + engineRegistry.ts (register/resolve/list/initialize/dispose, snapshot, subscribe). Consolidated from SOURCE's 7 registry/capability files, reshaped from its async extension model to the target's synchronous parent/child creation. Verified 8 behaviors with a temporary suite (deleted; permanent tests are S5) — it caught a real defect where a child could not dispose its own capability, now fixed. Checks green, committed 778b197.
- P2 S1 done — module-system scaffold + operationResult.ts (one success/degraded/failure shape, replacing SOURCE's three near-identical result unions) + engineContracts.ts (identity brands, capability tokens, diagnostics, outbound ports, parent/child definitions, lifecycle + snapshot). Consolidated from SOURCE's 8 contracts/* files. Two deliberate departures per LOGIC §5/§11: dropped surface vocabulary (SOURCE hardcoded admin/storefront into the generic runtime) and dropped UI vocabulary (navigation/route/label/icon/component contributions). Phase 2 activated. Checks green, committed 60bd877.
- P1 S6 done — index.ts barrel + domainRules.test.ts (17 protected-behavior tests). Installed minimal test toolchain (vitest, typescript, @types/node) — first deps in repo. Replaced structuredClone (needs DOM lib, breaks domain purity) with a pure JSON deep clone in finance.ts; added a test asserting deep-clone identity. typecheck + tests now run inside npm run check. ALL FOUR checks green. **P1-domain DONE.**
